2026-04-13

- Penny next-cycle refactor landed: direct-intent parsing/reply helpers, direct tool-assist logic, and tool modules were extracted out of `lyra-prototype/server.js`; targeted docs and tests were updated; follow-up QA harness cleanup also landed.
- User preference to preserve for future testing and routing: use heavier 31B models for companion chat, and prefer the smaller E4B model for bounded tool-use flows/QA where possible.
- More specific chat-lane preference: tool calls should stay on `google/gemma-4-e4b`, but chat should keep an easy choice between `google/gemma-4-31b` Q8 and `unsloth/gemma-4-31b-it` Q6; if a single default is needed, prefer the Google Q8 model unless latency makes that feel stupid.
- LM Studio automation note: automatic model swapping works safely with `lms unload/load` for `unsloth/gemma-4-31b-it`, so use Q6 as the practical automated chat-model path for testing; avoid auto-testing the heavier Q8 `google/gemma-4-31b` unless specifically needed because agentic/tool-heavy runs tend to time out or trip resource guardrails.
