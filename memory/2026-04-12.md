2026-04-12

## Imported from `lyra-prototype/memory/2026-04-12.md`

- Added a lightweight idle chat-field screensaver pass: more floating Penny/tech symbols, slow bouncing motion, and stronger presence when the thread is empty with softer background motion once messages are on screen.
- Extended the idle decor field so it can size to the full scrollable chat area instead of only the initial visible panel.
- Fixed Penny's repo/path discovery bug: folder-name questions like `Penny's Playground` now route to `list_project_files` instead of incorrectly using `search_project_text`.
- Added `big ass file to manageable chapters.md` as a plain-English note about turning giant ugly source blobs into cleaner indexed chapter files before retrieval.
- Raised Penny's default LM Studio request budget from 15 minutes to 30 minutes so long 31B local/tool turns are less likely to die right before finishing.
- Added `Today's Plan.md` as the wake-up handoff doc focused on the next two real branches: deeper unsanitized Penny voice refinement and deciding whether OpenClaw shadow mode is worth pursuing for agentic work.
- Added two follow-up reminders to `Today's Plan.md`: tighten Penny so she only implies writes/verifications that actually happened, and keep looking for ways to tighten fast deterministic tool routing.
- Reviewed the April 12, 2026 LocalLLaMA "AI-slop mess" thread and wrote `LOCAL_LLAMA_THREAD_FINDINGS.md` to map the thread's advice onto Penny's current architecture, testing, docs, and OpenClaw boundary.
- Reviewed the RYS / LLM Neuroanatomy links plus the related LocalLLaMA thread and wrote `RYS_FOLLOWUP_REVIEW.md`, focusing on what newly applies to Penny, whether the previous maintainability advice was actually followed, and where the repo still needs tests, modularization, and better artifact policy.
- Started the PM-note follow-through pass: current next slice is `README.md` truth refresh, quarantining the misleading checkpoint `server.js`, and extracting `mergeMemoryState` + memory consolidation logic out of `server.js` into a testable module before the next context compaction loses the thread.
