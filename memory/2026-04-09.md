## Penny UI acting pass

- Final stubborn left-side hair wedge in the chamber sprite was tucked in by shortening and rounding the cheek lock in `lyra-prototype/public/app.js`.
- Expanded Penny from 3 to 4 turn-based acting variants per mood so `calm`, `happy`, `excited`, `thinking`, and `surprised` can read more like personality states than one repeated sticker.
- Current acting direction is: soft, smug, casual-chaos, heart-thief, hype, gremlin, locked-in, flustered.
- Verified in browser without stressing LM Studio by using the `debugMood`, `debugTurns`, and `debugIdle=1` query params.
- Next best improvement is not more anatomy cleanup. It is either:
  - stronger bespoke reaction sprites, or
  - actual sprite assets / layered illustrations if we want a bigger jump in quality.
- Added the first actual acting pass after anatomy cleanup:
  - `happy` has a sweeter closed-eye `HEART THIEF` read with a floating heart note
  - `excited` leans more gremlin/show-off
  - `thinking` reads more like narrowed-eye "reading you" instead of neutral focus
  - `surprised` can now read flustered instead of only startled
- Left sleeve color was separated from the white coat with a dedicated `sleeveLight` gradient so the front arm no longer visually merges into the coat panel.
- Removed the dark center torso strip from Penny's outfit in `lyra-prototype/public/app.js` because it was reading like a black tie and visually pulling her head off-center in the chamber view.
- Fixed the right arm color mismatch across all mood sprites by changing the right-sleeve shapes from `coatAccent` to `sleeveLight`, so Penny's sleeves now read consistently on both sides.
- Added `lyra-prototype/PENNY_UI_HANDOFF.md` as a clean restart/handoff document for a fresh chat or Cursor session focused on Penny's acting/sprite pass.
