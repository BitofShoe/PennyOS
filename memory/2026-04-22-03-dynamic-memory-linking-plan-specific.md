# 2026-04-22 - Dynamic Memory Linking Plan-Specific Journal

- This journal was created because the main April 22 daily journal is already dense.
- Source plan read closely: `lyra-prototype/docs/plans/penny-post-tier1-bounded-aliveness-plans/03-dynamic-memory-linking-plan.md`.
- Slice in this chat: L0 docs/current-law note only.
- Core rule preserved: a memory link is a retrieval/navigation hint, not proof that either side is true.
- L0 target files: `README.md`, `ARCHITECTURE.md`, `CODEBASE.md`, `docs/README.md`, and `docs/plans/penny-dynamic-memory-linking-plan-2026-04-22.md`.
- Behavior changed: high-level docs and the docs index now route future agents to the dynamic-linking boundary before implementation.
- Behavior not changed: no runtime code, no prompt bridge, no archive ranking change, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB or universal memory index, and no broad project/research/open-loop link scoring.
- Next correct slice after L0 is L1 schema and pure helpers, then fixture traces and correction-link shadows. Do not jump straight to active scoring.
- Verification completed before commit: `git diff --check` passed, and staged `git diff --cached --check` passed with the new plan note and this journal included.

## Slice L1 - Memory link schema and pure helpers

- Slice L1 landed as schema/pure-helper work only.
- Added `lyra-prototype/lib/penny-memory-links.js` with `penny-memory-links.v1`, the v1 relation enum, support-state normalization, safe authority-effect normalization, link-set artifacts, summaries, item lookup, directed-link inversion, and validation receipts.
- Added `lyra-prototype/test/penny-memory-links.test.js` covering valid/invalid relations, safe authority defaults, summary counts, directed vs bidirectional lookup, artifact behavior, and static/semantic candidate-only downgrade behavior.
- Behavior changed: Penny can now represent, normalize, summarize, validate, and inspect advisory memory links in tests/fixtures.
- Behavior not changed: no runtime wiring, no archive ranking/scoring, no prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no default feature flag, and no project-thread/open-loop/research-pattern scoring activation.
- Guardrail preserved: candidate-only/static/semantic links normalize as advisory support and cannot retain `current-truth-boost` style authority effects.
- Verification before journal update: `node --check lib/penny-memory-links.js` passed, `node --check test/penny-memory-links.test.js` passed, `node --test test/penny-memory-links.test.js` passed (`7 passing`), and `git diff --check` passed.

## Slice L2 - Deterministic correction-link builder

- Slice L2 landed as deterministic correction-link builder work only.
- Added `lyra-prototype/lib/penny-memory-link-policy.js` with `penny-correction-link-builder.v1` and `buildCorrectionLinks`.
- Added `lyra-prototype/test/penny-memory-link-policy.test.js` covering brass fox -> copper rabbit, oolong -> lapsang souchong, silver watch -> gold watch, weak candidate/static support, and incomplete correction holdback.
- Behavior changed: fixture/policy helpers can now build inspectable correction link sets with `current-correction-for`, `stale-prior-of`, and `correction-of` links plus a bounded correction trace.
- Behavior not changed: no runtime wiring, no archive ranking/scoring, no prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no default feature flag, and no project-thread/open-loop/research-pattern scoring activation.
- Guardrails preserved: explicit correction support can encode future-gated `current-truth-boost`, `stale-current-penalty`, or `do-not-render-as-current` hints, but the artifact marks `behaviorChanged: false` and `scoringActive: false`. Candidate-only/static/semantic correction links stay advisory and cannot become verified support or current-truth boosts.
- Verification before journal update: `node --check lib/penny-memory-link-policy.js` passed, `node --check test/penny-memory-link-policy.test.js` passed, `node --test test/penny-memory-links.test.js test/penny-memory-link-policy.test.js` passed (`12 passing`), and `git diff --check` passed.

## Slice L3 - Link-aware candidate trace, no scoring changes

- Slice L3 landed as optional candidate-trace metadata only.
- Added `penny-memory-link-trace.v1` via `buildMemoryLinkTraceForItem` in `lyra-prototype/lib/penny-memory-links.js`, with bounded incoming/outgoing links, camel-case `relationSummary`, authority-effect listing, and explicit advisory/no-truth/no-scoring/no-behavior-change receipts.
- Updated `lyra-prototype/lib/penny-memory-archive.js` so `buildArchiveContext` can attach correction link metadata to `retrieval.candidateTrace` only when `includeCandidateTraceLinks` is requested. The archive path derives only active correction/contradiction links for trace inspection and can also normalize supplied trace links; selection, rendering, rank, prompt context, and archive scoring are unchanged.
- Updated `lyra-prototype/lib/penny-candidate-survival-qa.js` so candidate-survival normalization preserves bounded `memoryLinks` metadata and summarizes link-trace candidate counts without treating links as answer-quality evidence.
- Behavior changed: optional candidate traces can now show advisory memory-link metadata for stale/current correction cases.
- Behavior not changed: no archive scoring/ranking change, no active link scoring gate, no runtime prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no broad project-thread/open-loop/research-pattern scoring activation, and no candidate-only/static/semantic links upgraded to verified support.
- Guardrails preserved: derived archive correction links use advisory archive support, so their authority effects remain trace-only (`none` in the archive correction test); explicit authority effects can still be inspected only when supplied by already-normalized explicit link data.
- Verification before journal update: changed-owner `node --check` passed, test-owner `node --check` passed, `node --test test/penny-memory-links.test.js test/penny-candidate-survival-qa.test.js` passed (`28 passing`), `node --test test/penny-memory-archive.test.js` passed (`42 passing`), exact focused L3 command `node --test test/penny-memory-archive.test.js test/penny-candidate-survival-qa.test.js test/penny-memory-links.test.js` passed (`70 passing`), and `git diff --check` passed.

## Slice L4 - Link fixture/QA runner

- Slice L4 landed as a fixture-only QA runner for dynamic memory links.
- Added `lyra-prototype/scripts/qa-penny-memory-links.js` with `penny-memory-links-fixture.v1`, deterministic `--fixture` mode, `--output` / `--generated-at` support, five L4 fixture cases, link-set traces, interpretation summaries, and explicit no-runtime/no-scoring/no-truth-promotion receipts.
- Added `lyra-prototype/test/penny-memory-links-script.test.js` covering fixture shape, case order, correction-vs-broad advisory behavior, weak semantic candidate handling, writer output, and fixture-only argument parsing.
- Added `npm run qa:memory:links`, which writes ignored artifacts at `lyra-prototype/output/memory-links-fixture-<stamp>.json`.
- Fresh artifact: `lyra-prototype/output/memory-links-fixture-2026-04-22T10-55-48-232Z.json`.
- Fresh artifact summary: 5 cases passing, 7 total links, 3 correction links, 4 broad advisory links, 2 correction authority-affecting hints, 0 broad authority-affecting links, 0 candidate-only verified-support links, 0 truth-proof links, 0 canonical-memory writes, 0 PromptTruth/tool-evidence changes, and scoring inactive.
- Behavior changed: repo now has a repeatable fixture/QA command for inspecting dynamic memory links across correction-chain, same-project-thread, open-loop-about, research-pattern-for, and related-but-weak cases.
- Behavior not changed: no runtime wiring, no archive ranking/scoring activation, no prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no active broad project-thread/open-loop/research-pattern scoring, and no candidate-only/static/semantic support promotion.
- Guardrails preserved: only the explicit correction chain carries future-gated authority hints; project-thread/open-loop/research-pattern links remain retrieval/navigation metadata with scoring inactive; weak semantic relation uses `related-but-weak` with `authorityEffect: none` and cannot become verified support.
- Verification before journal update: `node --check scripts/qa-penny-memory-links.js` passed, `node --check test/penny-memory-links-script.test.js` passed, `package.json` parsed as JSON, focused `node --test test/penny-memory-links.test.js test/penny-memory-link-policy.test.js test/penny-memory-links-script.test.js` passed (`17 passing`), `npm run qa:memory:links` passed and wrote the artifact above, artifact spot-check passed, and `git diff --check` passed.

## Slice L5 - Link-aware scoring shadow

- Slice L5 landed as inactive link-aware scoring shadow only.
- Added `penny-memory-link-shadow-score.v1` in `lyra-prototype/lib/penny-memory-link-policy.js` with `scoreMemoryLinkShadowForCandidate` and `scoreMemoryLinkShadowForCandidates`.
- The shadow receipt reports `currentCorrectionBoost`, `stalePriorPenalty`, `sameProjectThreadBoost`, `openLoopRelevanceBoost`, `weakRelationPenalty`, reasons, active/shadow rank metadata, and `wouldChangeRank`, while keeping `active: false`, `behaviorChanged: false`, `truthProof: false`, and no PromptTruth/tool-evidence/canonical-memory effects.
- Updated `lyra-prototype/lib/penny-memory-archive-policy.js` so `scoreArchiveCandidateWithProfile` returns a `linkShadowScore` receipt when links are supplied, but active score, active reasons, scoring profile behavior, selection, and rendering remain unchanged.
- Behavior changed: policy/fixture code can now compute how explicit correction links and broad advisory links would help or hurt candidate ranking in shadow.
- Behavior not changed: no active archive ranking/scoring change, no live runtime wiring, no prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no correction scoring gate activation, no broad project-thread/open-loop/research-pattern active scoring, and no candidate-only/static/semantic support promotion.
- Guardrails preserved: only explicit correction support produces current/stale authority shadows; candidate-only/static/semantic links remain non-proof; same-project/open-loop broad links are weaker advisory shadow components; weak links cannot override verified source authority.
- Verification before journal update: changed-owner `node --check` passed, focused `node --test test/penny-memory-archive-policy.test.js test/penny-memory-link-policy.test.js` passed (`21 passing`), adjacent `node --test test/penny-memory-links.test.js test/penny-memory-link-policy.test.js test/penny-memory-archive-policy.test.js test/penny-memory-archive.test.js` passed (`71 passing`), `npm run qa:memory:links` passed and wrote `lyra-prototype/output/memory-links-fixture-2026-04-22T11-03-19-135Z.json`, and `git diff --check` passed.

## Slice L6 - Link-aware candidate survival artifact

- Slice L6 landed as candidate-survival artifact/reporting work only.
- Added per-case `linkAnalysis` in `lyra-prototype/lib/penny-candidate-survival-qa.js` with `expectedCandidateLinks`, `staleCandidateLinks`, `linkFailureMode`, `shadowRankDelta`, `verdict`, inactive link-shadow receipts, and explicit `advisoryOnly` / `truthProof: false` / `behaviorChanged: false` / `scoringActive: false` guardrails.
- Added artifact-level `linkAnalysisSummary` counts for failure modes, verdicts, expected/stale link counts, and candidate-only verified-support count.
- Updated `lyra-prototype/scripts/qa-penny-memory.js` so archive-unit candidate-survival QA requests bounded candidate-trace links (`includeCandidateTraceLinks: true`, link trace limit 6).
- Added focused tests for missing correction links, stale-prior link reporting, link-shadow rank improvement, and candidate-only/semantic correction links staying weak and non-verified.
- Fresh artifact: `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T11-14-29-992Z.json`.
- Fresh artifact summary: 8 archive-unit cases, `includeCandidateTraceLinks: true`, no live model calls, cleanup clean, link analysis `weak-link: 1`, `missing-link: 1`, `link-would-help: 0`, and `candidateOnlyVerifiedSupportCount: 0`.
- Behavior changed: candidate-survival archive-unit artifacts now show whether correction links are present, missing, weak, ignored, or hypothetically helpful, while preserving inactive shadow-scoring receipts.
- Behavior not changed: no active archive ranking/scoring, no correction scoring gate activation, no runtime prompt bridge, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no broad project-thread/open-loop/research-pattern scoring, and no candidate-only/static/semantic support promotion.
- Guardrails preserved: explicit memory remains canonical; archive-derived links are advisory and can show `weak-link` without becoming current-truth support; candidate-only/static/semantic links keep `candidateOnlyVerifiedSupport: false`.
- Verification before journal update: changed-owner `node --check` passed, focused `node --test test/penny-candidate-survival-qa.test.js test/penny-memory-qa-script.test.js` passed (`40 passing`), `npm run qa:memory:candidate-survival` passed and wrote the artifact above, and artifact spot-check confirmed link summaries plus cleanup.

## Slice L7 - Gated conservative correction-link scoring

- Slice L7 landed as active correction-link archive scoring behind `PENNY_MEMORY_LINK_SCORING=correction-v1`.
- Added `penny-memory-link-active-score.v1` in `lyra-prototype/lib/penny-memory-link-policy.js`, with normalized modes `off`, `shadow`, and `correction-v1`.
- Default behavior remains shadow-only; `correction-v1` is required before any link score can affect active archive ranking.
- Active L7 effects are intentionally smaller than L5 shadow scores: explicit `current-correction-for` can add a modest current correction boost, and explicit `stale-prior-of` can apply a stale-prior penalty.
- Wired `lyra-prototype/lib/penny-memory-archive-policy.js` and `lyra-prototype/lib/penny-memory-archive.js` so supplied memory-link sets can affect archive candidate ranking only through `correction-v1`.
- Candidate traces now expose the active memory-link scoring mode and `memoryLinkCorrectionScore` when the gate is enabled; prompt/rendered-memory limits are unchanged.
- Behavior changed: when an explicit correction link set is supplied and `memoryLinkScoring` / `PENNY_MEMORY_LINK_SCORING` is `correction-v1`, the current correction can outrank a stale prior and the stale value can be held out of rendered archive context.
- Behavior not changed: default archive ranking remains unchanged; link shadows remain advisory; candidate-survival QA remains non-active/shadow; no PromptTruth expansion; no `toolEvidenceReceipt` change; no runtime voice change; no graph DB/universal index; no memory store migration; no broad same-project/open-loop/research-pattern scoring; no prompt-limit increase; no advisory memory promotion.
- Guardrails preserved: active scoring ignores same-project-thread, open-loop, research-pattern, weak, static, semantic, and candidate-only authority attempts. Candidate-only/static/semantic correction links still cannot become verified support or truth proof.
- Fresh artifact: `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T11-22-06-477Z.json`.
- Fresh artifact summary: 8 archive-unit cases, no live model calls, cleanup clean, link analysis `missing-link: 1`, `weak-link: 1`, `candidateOnlyVerifiedSupportCount: 0`, `behaviorChanged: false`, `scoringActive: false`, and `truthProof: false`.
- Verification before journal update: changed-owner `node --check` passed; `node --test test/penny-memory-link-policy.test.js` passed (`10 passing`); focused `node --test test/penny-memory-link-policy.test.js test/penny-memory-archive-policy.test.js` passed (`24 passing`); `node --test test/penny-memory-archive.test.js` passed (`44 passing`); slice command `node --test test/penny-memory-archive-policy.test.js test/penny-memory-archive.test.js test/penny-candidate-survival-qa.test.js` passed (`81 passing`); `npm run qa:memory:candidate-survival` passed and wrote the artifact above; and `git diff --check` passed.

## Slice L8 - Reflection-generated link suggestions

- Slice L8 landed as review-gated reflection link suggestions only.
- Added `penny-session-reflection-link-suggestions.v1` in `lyra-prototype/lib/penny-session-reflection.js`, wrapping normalized `penny-memory-links.v1` link sets for reflection output without adding a store, graph DB, prompt bridge, or active ranking path.
- Reflection can now carry advisory suggestions for `same-project-thread`, `follow-up-to`, `implements-plan`, `research-pattern-for`, and `open-loop-about`.
- Every reflection-generated link suggestion is forced to `createdBy: reflection`, `reviewState: needs-review`, `advisoryOnly: true`, `truthProof: false`, `behaviorChanged: false`, and `scoringActive: false`.
- Open-loop reflection updates can name `memoryLinkTargets`; source-backed updates generate review-gated `open-loop-about` link suggestions with a session-reflection source receipt.
- Unsupported sensitive or user-fact link attempts are held back with `unsupported-sensitive-or-user-fact-link` rather than becoming link suggestions.
- Updated `lyra-prototype/scripts/qa-penny-session-reflection.js` so the fixture artifact now includes static-plan-to-frame-budget `same-project-thread` / `research-pattern-for` suggestions and an open-loop-derived `open-loop-about` suggestion.
- Fresh artifact: `lyra-prototype/output/session-reflection-fixture-2026-04-22T11-29-19-350Z.json`.
- Fresh artifact summary: 5 passing fixture cases; `reflectionMemoryLinkSuggestionCount: 3`; `reflectionMemoryLinkHeldBackCount: 0`; `reflectionMemoryLinkNeedsReviewCount: 3`; `reflectionMemoryLinkScoringActive: false`; `reflectionMemoryLinkTruthProof: false`; project and open-loop links review-gated.
- Behavior changed: session reflection artifacts can now expose review-gated advisory link suggestions and source-backed open-loop link targets for later QA/compare work.
- Behavior not changed: no default runtime prompt rendering, no archive ranking change, no broad project/open-loop/research active scoring, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no canonical memory write, no advisory memory promotion, and no candidate-only/static/semantic verified-support upgrade.
- Guardrails preserved: active `correction-v1` scoring still ignores reflection project/research/open-loop links; broad L8 suggestions may show shadow components but cannot activate ranking or make either endpoint true.
- Verification before journal update: changed-owner `node --check` passed for `lib/penny-session-reflection.js`, `scripts/qa-penny-session-reflection.js`, and touched tests; focused L8 command `node --test test/penny-session-reflection.test.js test/penny-memory-links.test.js test/penny-memory-link-policy.test.js` passed (`35 passing`); fixture regression `node --test test/penny-session-reflection-script.test.js` passed (`9 passing`); `npm run qa:session-reflection` passed and wrote the artifact above; and `git diff --check` passed.
- Post-journal full unit sweep also passed: `npm test` reported `768 passing, 0 failing, 3 todo`.

## Slice L9 - Dynamic link compare harness

- Slice L9 landed as a fixture-only dynamic memory link compare harness.
- Added `lyra-prototype/scripts/eval-penny-memory-links-compare.js` with `penny-memory-links-compare.v1`, modes `links-off`, `links-trace-only`, `links-shadow`, `correction-links-active`, and `project-links-shadow`.
- Added `lyra-prototype/test/penny-memory-links-compare.test.js` and `npm run eval:memory-links`.
- Fresh artifact: `lyra-prototype/output/memory-links-compare-2026-04-22T11-38-30-855Z.json`.
- Fresh artifact summary: paired verdict `correction-links-active`; enablement recommendation `eligible-for-gated-correction-v1-review`; default scoring recommendation `keep-default-shadow`; project/research/open-loop recommendation `shadow-only-until-separately-measured`; `correctionCurrentTruthWins: 2`; `staleMemoryRegressions: 0`; `sourceAuthorityFailures: 0`; `continuityWins: 1`; `overclaimRegressions: 0`; `promptTokenDelta: 0`; `trustVerdict: pass`.
- Behavior changed: repo now has a repeatable compare command and artifact for measuring dynamic memory link modes against correction, stale-memory, candidate-only/static/semantic, project-thread, open-loop, research-pattern, and weak-link boundary fixtures.
- Behavior not changed: no live runtime prompt bridge, no default scoring flip, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no graph DB/universal index, no memory store migration, no canonical memory write, no advisory memory promotion, no live LM Studio/server calls, no prompt-limit increase, and no broad project/open-loop/research active scoring.
- Guardrails preserved: only explicit correction links win under the `correction-v1` comparison; candidate-only/static/semantic links do not become verified support; project-thread/open-loop/research links produce shadow continuity evidence only and stay inactive until separately measured.
- Verification before final commit: changed-owner `node --check` passed; focused `node --test test/penny-memory-links-compare.test.js` passed (`7 passing`); `npm run eval:memory-links` passed and wrote the artifact above; adjacent focused `node --test test/penny-memory-links-compare.test.js test/penny-memory-links-script.test.js test/penny-memory-link-policy.test.js test/penny-memory-archive-policy.test.js test/penny-aliveness-qa.test.js` passed (`49 passing`); `git diff --check` passed; and full `npm test` passed (`775 passing, 0 failing, 3 todo`).

## Semantic Identity Slice 4 - Authority Domains

- Slice 4 landed as pure authority-domain contract work only.
- Added `lyra-prototype/lib/penny-semantic-domains.js` with `penny-semantic-domain-registry.v1`, registered local domains for explicit memory, session archive, global archive, research ledger, static candidate, open loop, tool evidence, document extraction, repo current law, runtime artifact, and fixture-only QA.
- Updated `lyra-prototype/lib/penny-semantic-claims.js` so claim normalization/validation now uses the domain registry for authority defaults, temporal defaults, renderability, canonicality, source-type mapping, and fail-closed unknown-domain checks.
- Added `lyra-prototype/test/penny-semantic-domains.test.js` covering canonical explicit memory, advisory archive/research/open-loop posture, candidate-only static candidates, repo-current-law not canonizing personal memory, tool evidence staying outside PromptTruth, source-type mapping, unknown-domain fail-closed behavior, and registry copy safety.
- Updated high-level docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md`.
- Behavior changed: Penny can now represent and test source authority domains as local contracts, and semantic claims reject unregistered domains or repo-current-law personal-memory overclaims.
- Behavior not changed: no runtime wiring, no retrieval/ranking change, no PromptTruth change, no `toolEvidenceReceipt` change, no prompt-limit increase, no memory promotion, no RDF/JSON-LD/SPARQL/triplestore/graph machinery, and no URI dereferencing.
- Guardrails preserved: explicit memory remains canonical; archive/research/open-loop/document/runtime domains stay advisory or non-live as declared; static candidates remain candidate-only; tool evidence remains sibling receipt evidence rather than a PromptTruth channel; unknown domains fail closed.
- Verification before journal update: `node --check lib/penny-semantic-domains.js`, `node --check lib/penny-semantic-claims.js`, `node --check test/penny-semantic-domains.test.js`, focused `node --test test/penny-semantic-domains.test.js` (`9 passing`), focused semantic contract command `node --test test/penny-semantic-ids.test.js test/penny-semantic-predicates.test.js test/penny-semantic-claims.test.js test/penny-semantic-domains.test.js` (`33 passing`), and full `npm test` (`808 passing, 0 failing, 3 todo`) passed.

## Semantic Identity Slice 5 - Structured Candidate Contract QA

- Slice 5 landed as structured candidate-contract QA only.
- Added `penny-structured-candidate-contract-qa.v1` inside `lyra-prototype/lib/penny-candidate-survival-qa.js`, with failure classes for right-object/wrong-predicate, stale object under right predicate, wrong temporal scope, candidate-only treated as verified, rendered-advisory treated as canonical, found-not-rendered, missing source id, unstable claim id, authority-domain mismatch, source-id mismatch, missing expected claim, and not-applicable cases.
- Candidate trace normalization can now preserve compact `semanticClaim` and `claimTreatment` metadata for QA artifacts. Source-sensitive and candidate-survival fixtures expose a `structuredCandidateContracts` summary, but this remains fixture/archive-unit QA evidence only.
- Updated focused docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md` so future agents do not mistake structured candidate contracts for graph authority or runtime truth.
- Fresh artifacts: `lyra-prototype/output/memory-qa-candidate-survival-fixture-2026-04-22T12-27-28-432Z.json`, `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T12-27-28-432Z.json`, and `lyra-prototype/output/memory-qa-source-sensitive-2026-04-22T12-27-28-441Z.json`.
- Fresh artifact summary: structured candidate-contract QA has 11 fixture cases, one valid current claim, one candidate-only-treated-as-verified case, one rendered-advisory-treated-as-canonical case, one source-id-mismatch case, `promptTruthExpanded: false`, and `toolEvidenceReceiptChanged: false`.
- Behavior changed: candidate/source-sensitive QA can now distinguish semantic correctness from string coincidence and can flag claim/source/authority/temporal contract failures before they become answer-quality claims.
- Behavior not changed: no runtime wiring, no archive retrieval/ranking change, no active scoring change, no PromptTruth expansion, no `toolEvidenceReceipt` change, no prompt-limit increase, no memory promotion, no RDF/JSON-LD/SPARQL/triplestore/graph machinery, no URI dereferencing, and no `server.js` change.
- Guardrails preserved: explicit memory remains canonical; candidate-only/static/semantic support cannot satisfy verified/canonical contracts; rendered advisory support cannot become canonical; structured claim IDs and source IDs are local QA contracts rather than fetchable authorities.
- Verification before journal update: changed-owner `node --check` passed; focused `node --test test/penny-candidate-survival-qa.test.js` (`25 passing`), `node --test test/penny-context-pressure-qa.test.js` (`9 passing`), `node --test test/penny-memory-qa-script.test.js` (`17 passing`), combined semantic/candidate command (`84 passing`), `npm run qa:memory:candidate-survival-fixture`, `npm run qa:memory:candidate-survival`, `npm run qa:memory:source-sensitive`, artifact spot-check, focused post-doc command (`51 passing`), full `npm test` (`810 passing, 0 failing, 3 todo`), and `git diff --check` passed.

## Semantic Identity Slice 6 - Source-ID Audit

- Slice 6 landed as fixture/local-audit source-ID continuity QA only.
- Added `lyra-prototype/lib/penny-semantic-source-audit.js` with `penny-semantic-source-audit.v1`, source-surface summaries, source ID validation, static cache/source mismatch checks, PromptTruth rendered-source checks, tool-evidence source checks, dynamic-link endpoint checks, semantic-claim source/claim ID drift checks, and explicit no-runtime/no-PromptTruth/no-tool-evidence/no-dereference guardrails.
- Added `lyra-prototype/scripts/qa-penny-semantic-source-audit.js`, `lyra-prototype/test/penny-semantic-source-audit.test.js`, and `npm run qa:semantic:source-audit`.
- Updated focused docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md`.
- Fresh artifact: `lyra-prototype/output/semantic-source-audit-2026-04-22T12-38-32-128Z.json`.
- Fresh artifact summary: 10 audited surfaces, 11 fixture items, `failureCount: 0`, `allSourceIdsPresent: true`, `cacheSourceMismatches: 0`, `renderedItemsWithSourceIds: 1`, `dynamicLinkMissingTargets: 0`, `promptTruthExpanded: false`, `toolEvidenceReceiptChanged: false`, and `uriDereferencing: false`.
- Behavior changed: Penny now has a repeatable QA artifact for proving fixture-shaped semantic/static/link/rendered/tool-evidence/candidate/claim surfaces still point back to stable source IDs, and for flagging missing IDs, unstable IDs, static cache/source mismatches, rendered-item source gaps, dynamic-link endpoint gaps, and semantic-claim source/claim drift.
- Behavior not changed: no runtime wiring, no retrieval/ranking change, no active scoring change, no PromptTruth expansion, no `toolEvidenceReceipt` change, no prompt-limit increase, no memory promotion, no RDF/XML/JSON-LD/SPARQL/triplestore/graph machinery, no URI dereferencing, no live model calls, and no `server.js` change.
- Guardrails preserved: explicit memory remains canonical; source-ID audit is provenance continuity evidence, not answer-quality proof or truth authority; candidate/static/semantic support remains discovery-only unless promoted by existing reviewed paths.
- Verification before journal update: changed-owner `node --check` passed; focused semantic contract command `node --test test/penny-semantic-ids.test.js test/penny-semantic-predicates.test.js test/penny-semantic-domains.test.js test/penny-semantic-claims.test.js test/penny-semantic-source-audit.test.js` passed (`38 passing`); adjacent candidate/script command `node --test test/penny-candidate-survival-qa.test.js test/penny-memory-qa-script.test.js test/penny-semantic-source-audit.test.js` passed (`47 passing`); `npm run qa:semantic:source-audit` passed and wrote the artifact above; package JSON parse check passed; and full `npm test` passed (`815 passing, 0 failing, 3 todo`).

## Semantic Identity Slice 7 - Dynamic Memory Linking Integration

- Slice 7 landed as semantic-contract metadata on dynamic memory links, with compatibility kept for the existing `penny-memory-links.v1` shape.
- Updated `lyra-prototype/lib/penny-semantic-predicates.js` with registered dynamic-link predicates for current correction, summaries, evidence/support, user-preference evidence, research patterns, and weak relatedness. Predicate IDs remain the behavior contract; labels alone still cannot drive behavior.
- Updated `lyra-prototype/lib/penny-memory-links.js` so normalized links can carry local semantic `linkId`, registered `predicateId`, structured `sourceClaimId` / `targetClaimId`, authority domain, source authority, support state, evidence receipts, ranking eligibility, and a non-canonical `penny-memory-link-semantic-contract.v1` receipt.
- Structured dynamic links now fail closed on unknown predicates, predicate-label-only behavior attempts, and missing semantic claim endpoints. Candidate-only/static/semantic links remain advisory and cannot canonize, auto-promote, or become verified support.
- Semantic correction inverse links are generated only when explicitly configured, and only for structured correction links with claim endpoints.
- Updated `lyra-prototype/lib/penny-semantic-source-audit.js` so source audit fixtures and checks understand semantic link IDs and structured link claim endpoints, including invalid-link-ID and missing-target diagnostics.
- Updated focused docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md`.
- Fresh artifacts: `lyra-prototype/output/semantic-source-audit-2026-04-22T12-58-59-229Z.json`, `lyra-prototype/output/memory-links-fixture-2026-04-22T12-58-59-243Z.json`, `lyra-prototype/output/memory-links-compare-2026-04-22T12-59-30-702Z.json`, and `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T12-59-30-805Z.json`.
- Fresh artifact summary: source audit `failureCount: 0`, `totalItems: 12`, `dynamicLinkMissingTargets: 0`, `promptTruthExpanded: false`, and `toolEvidenceReceiptChanged: false`; memory-link fixture `passingCaseCount: 5`, `totalLinks: 7`, `candidateOnlyVerifiedSupportLinks: 0`, and `scoringActive: false`; candidate-survival QA kept `weakLink: 1`, `missingLink: 1`, and `candidateOnlyVerifiedSupportCount: 0`; compare artifact kept paired verdict `correction-links-active`, default recommendation `keep-default-shadow`, `promptTokenDelta: 0`, and `trustVerdict: pass`.
- Behavior changed: dynamic memory link helpers, fixtures, QA, and source-audit surfaces can now represent source-aware semantic link contracts with local link IDs, registered predicates, claim endpoints, authority/support metadata, and explicit ranking eligibility.
- Behavior not changed: no runtime prompt bridge, no default ranking flip, no active scoring expansion, no PromptTruth expansion, no `toolEvidenceReceipt` change, no runtime voice change, no memory store migration, no canonical memory write, no advisory memory promotion, no prompt-limit increase, no live LM Studio/server calls, no `server.js` change, no RDF/XML/JSON-LD/SPARQL/triplestore/ontology inference/Linked Data publishing/graph DB replacement, and no URI dereferencing.
- Guardrails preserved: explicit memory remains canonical; dynamic semantic links are provenance/support contracts only; candidate discovery is not truth authority; source audit and compare artifacts remain QA evidence rather than proof that a retrieved claim is true.
- Verification before final commit: changed-owner `node --check` passed; focused semantic/link/source command `node --test test/penny-memory-links.test.js test/penny-semantic-predicates.test.js test/penny-semantic-source-audit.test.js` passed (`26 passing`); adjacent memory-link scripts command passed (`21 passing`); semantic contract command passed (`39 passing`); archive regression passed (`44 passing`); candidate-survival script regression passed (`42 passing`); post-fix memory-link/archive command passed (`80 passing`); post-fix candidate/link script command passed (`53 passing`); `npm run qa:semantic:source-audit`, `npm run qa:memory:links`, `npm run qa:memory:candidate-survival`, and `npm run eval:memory-links` passed; artifact spot-check passed; and full `npm test` passed (`820 passing, 0 failing, 3 todo`).

## Semantic Identity Slice 8 - Semantic Claim Traces In Candidate Survival

- Slice 8 landed as candidate-survival QA trace enrichment only.
- Updated `lyra-prototype/lib/penny-memory-archive-policy.js` and `lyra-prototype/lib/penny-memory-archive.js` so archive candidates can preserve structured claim payloads into retrieval `candidateTrace` entries while keeping prompt-facing `archiveContext` items claim-free.
- Updated `lyra-prototype/lib/penny-candidate-survival-qa.js` with sibling `penny-semantic-claim-trace.v1` archive-unit metadata. The trace labels structured vs unstructured advisory candidates, expected claim matches, right-object/wrong-predicate matches, stale predicate/object matches, candidate-only claims, selected/rendered claim candidates, and explicit no-PromptTruth/no-tool-evidence/no-scoring/no-truth guardrails.
- Updated focused tests in `lyra-prototype/test/penny-memory-archive.test.js` and `lyra-prototype/test/penny-candidate-survival-qa.test.js` for claim pass-through, unstructured advisory labels, wrong-predicate visibility, static candidate-only claims, candidate channels, and PromptTruth/tool-evidence boundaries.
- Updated high-level docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md`.
- Behavior changed: archive-unit candidate-survival artifacts can now show structured semantic claim traces beside existing survival/link/contract summaries, so reviewers can distinguish "found copper rabbit as the current mascot" from "found copper rabbit in unrelated text."
- Behavior not changed: no runtime prompt bridge, no prompt-facing archiveContext claim graph, no PromptTruth expansion, no `toolEvidenceReceipt` change, no default ranking/scoring change, no prompt-limit increase, no memory promotion, no live LM Studio/server calls, no `server.js` change, no RDF/XML/JSON-LD/SPARQL/triplestore/ontology/Linked Data/graph DB work, and no URI dereferencing.
- Guardrails preserved: explicit memory remains canonical; archive/static/semantic candidates remain advisory unless promoted by existing reviewed paths; semantic claim traces are retrieval-path QA metadata, not proof, truth authority, or canonical memory.
- Fresh artifacts: `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T13-14-08-873Z.json` and `lyra-prototype/output/memory-qa-candidate-survival-fixture-2026-04-22T13-14-08-873Z.json`.
- Fresh artifact summary: archive-unit has 8 cases, `semanticClaimTraceSummary.schema: penny-semantic-claim-trace.v1`, `structuredClaimCandidateCount: 14`, `unstructuredAdvisoryCandidateCount: 0`, `promptTruthExpanded: false`, `toolEvidenceReceiptChanged: false`, `truthProof: false`, `scoringActive: false`, and `defaultPromptLimitsRaised: false`.
- Verification before final commit: changed-owner `node --check` passed; focused `node --test test/penny-candidate-survival-qa.test.js` passed (`27 passing`); focused `node --test test/penny-memory-archive.test.js` passed (`45 passing`); adjacent candidate/archive/script command passed (`103 passing`); `npm run qa:memory:candidate-survival` passed and wrote the archive-unit artifact above; `npm run qa:memory:candidate-survival-fixture` passed and wrote the fixture artifact above; artifact spot-check passed; full `npm test` passed (`823 passing, 0 failing, 3 todo`); and `git diff --check` passed.

## Semantic Identity Slice 9 - Prompt Rendering Authority Guardrails

- Slice 9 landed as compact rendered-claim PromptTruth guardrails.
- Updated `lyra-prototype/lib/penny-prompttruth.js` so PromptTruth channels can normalize bounded `renderedClaims[]` entries with only `renderedClaimId`, `domainId`, `sourceAuthority`, `supportState`, and `temporalScope`.
- Updated `lyra-prototype/lib/penny-memory.js` so rendered session/global archive context can preserve those compact claim authority labels only when the archive item actually renders.
- Updated `lyra-prototype/lib/penny-memory-archive.js` so archive context items can carry compact `renderedClaim` authority metadata while raw claim payloads remain in retrieval `candidateTrace` only.
- Updated focused tests in `lyra-prototype/test/penny-prompttruth.test.js`, `lyra-prototype/test/penny-memory.test.js`, and `lyra-prototype/test/penny-memory-archive.test.js`.
- Updated high-level docs in `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md`.
- Behavior changed: rendered archive claim summaries now keep compact authority labels in PromptTruth, so reviewers can see that a rendered claim was advisory/session-archive/current without exposing the raw claim graph.
- Behavior not changed: no new PromptTruth channel, no raw semantic graph in PromptTruth, no raw claim graph in prompt-facing archive context, no `toolEvidenceReceipt` change, no default ranking/scoring change, no prompt-limit increase, no memory promotion, no runtime voice change, no live LM Studio/server calls, no `server.js` change, no RDF/XML/JSON-LD/SPARQL/triplestore/ontology/Linked Data/graph DB work, and no URI dereferencing.
- Guardrails preserved: held-back, stale, fixture-only, and candidate-only claims stay out of `renderedClaims[]`; semantic claim traces remain sibling retrieval/QA metadata; dynamic links, source graph internals, static similarity traces, tool evidence, and raw candidate traces stay outside PromptTruth.
- Fresh artifact: `lyra-prototype/output/memory-qa-candidate-survival-archive-unit-2026-04-22T13-23-24-848Z.json`.
- Fresh artifact summary: 8 archive-unit cases, `semanticClaimTraceSummary.schema: penny-semantic-claim-trace.v1`, `promptTruthExpanded: false`, and `toolEvidenceReceiptChanged: false`.
- Verification before final commit: changed-owner `node --check` passed; focused `node --test test/penny-prompttruth.test.js test/penny-memory.test.js test/penny-memory-archive.test.js` passed (`80 passing`); adjacent semantic/archive command passed (`120 passing`); `npm run qa:memory:candidate-survival` passed and wrote the artifact above; full `npm test` passed (`827 passing, 0 failing, 3 todo`); and `git diff --check` passed.
