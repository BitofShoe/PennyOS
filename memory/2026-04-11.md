## Penny LM Studio optimization pass

- Implemented LM Studio native stateful chat support in `lyra-prototype/server.js` using the REST endpoint at `/api/v1/chat`.
- Added persisted per-session `lmStudioThread` state so Penny can reuse `previous_response_id` instead of rebuilding the whole conversation every turn.
- Set local LM Studio transport default to `auto`, which now tries native stateful chat first, then falls back safely to OpenAI-compatible `chat/completions`, then `/responses` if needed.
- Added native LM Studio base reporting to Penny status output.
- Verified live against local LM Studio:
  - direct `/api/v1/chat` request worked
  - `previous_response_id` continuation worked
  - Penny temp server on port `4318` completed two same-session replies and persisted updated `lmStudioThread.responseId`
- Main remaining high-value speed/polish step is streaming, so the UI can show progress before the full reply finishes.

## Penny LM Studio streaming pass

- Implemented SSE streaming from Penny to the browser in `lyra-prototype/server.js` and `lyra-prototype/public/app.js`.
- Penny now supports streamed local replies over the native LM Studio stateful chat route and also over fallback `chat/completions` and `/responses` routes.
- Added stage/status events so the UI can show short states like `link open`, `thinking`, and `replying` while a response is in progress.
- Restarted the live Penny server on port `4317` onto the streaming build.
- Verified live against the actual `4317` server with SSE:
  - received `status` events
  - received many `message.delta` chunks instead of one final blob
  - received final `done` payload with the assistant text and updated memory
- Fixed one follow-up bug in fallback chunk handling where leading spaces were being trimmed from streamed chat deltas.

## Penny installed-model dropdown fix

- The model dropdown was only showing the narrow LM Studio runtime list because Penny was reading `/v1/models`, which only reflected currently surfaced server models.
- Verified locally with `lms ls --json` that LM Studio had four LLMs installed on disk while Penny only saw the runtime subset.
- Updated Penny to read installed LLMs from `lms ls --llm --json` and expose them as `installedModels` in the LM Studio status payload.
- Updated the frontend model picker to prefer `installedModels` and to normalize alias differences between runtime IDs and publisher-prefixed model keys.
- Restarted the live Penny server on port `4317` after the fix.

## Penny empty-state / sprite / voice follow-up

- Removed the startup copy in the chat empty state so the opening screen is just the floating decor and chamber art.
- Fixed the chamber sprite cycle bug: Penny now actually alternates between the two mood variants based on turn count instead of reusing one pose per mood.
- Verified in a real browser via Playwright that `happy` swaps from `penny-mood-happy.png` / `CHARM MODE` to `penny-mood-happy-2.png` / `SOFT SPOT`.
- Strengthened both local prompt paths so Penny is explicitly allowed to swear when it makes the line hotter, funnier, meaner, or more natural.
- Confirmed with a live server smoke reply that the updated local prompt can now produce profanity again when directly invited.

## Penny chamber art pack swap

- The main vessel display was still using the older full illustration `penny-mood-*.png` pack even after the rest of the UI had moved to the chibi Penny 3.0 direction.
- Repointed the chamber mood variants to the newer chibi art set under `public/sprites/decor/`.
- Added cache-busting query strings to `public/index.html` for `app.js` and `styles.css` so browser sessions stop hanging onto stale frontend assets.
- Verified via Playwright that `smug` now resolves to:
  - turn 1: `/sprites/decor/chibi-avatar-smug.png`
  - turn 2: `/sprites/decor/chibi-penny-wink.png`

## Penny image guardrails + first tool-use pass

- Added client-side image prep in `lyra-prototype/public/app.js`:
  - images are resized/compressed before upload
  - the composer now shows upload feedback and clearer image errors
  - chat/consolidation payloads now strip historical base64 images instead of re-sending them every turn
- Added backend image validation in `lyra-prototype/server.js`:
  - bounded request-body size
  - strict base64 image parsing + MIME checks
  - hard size ceiling for decoded image payloads
  - clearer user-facing failures when LM Studio rejects vision input or when `/responses` would drop images
- Added a first local tool-use backend pass in `lyra-prototype/server.js`:
  - tools for runtime status, listing files, reading files, searching text, and tailing logs
  - direct fast-path tool routing for obvious asks like runtime/model status
  - broader model-driven tool loop for more open-ended project inspection requests
  - streamed tool events now reach the UI, and completed assistant messages can show which tools Penny used
- Verified on a disposable server instance at `http://127.0.0.1:4321`:
  - oversized image payloads now fail with HTTP `413`
  - runtime/model questions now return immediately through the new tool path with `meta.toolsUsed`
- Still worth checking next:
  - how the slower `read_project_file` / broader tool loop feels on the current local model, since nuanced file-summary requests can still be limited by LM Studio generation speed

## Penny file attachment + coding tools pass

- Added a real text/code file attachment affordance to Penny's composer UI beside the camera / emoji controls.
- File attachments now support a preview chip in the composer and a compact file badge in the user message row.
- Penny now accepts one text/code file attachment per turn in addition to the existing image flow.
- Backend validation now rejects unsupported file types, binary-looking payloads, and oversized text attachments.
- Added safer first-wave coding tools to `lyra-prototype/server.js`:
  - `write_project_file`
  - `replace_in_project_file`
  - `run_node_check`
  - `get_git_status`
- Direct tool routing now handles at least three fast cases without waiting on a long generative pass:
  - runtime/model status
  - `node --check` syntax checks for explicit files
  - git status
- Verified on a disposable server at `http://127.0.0.1:4322`:
  - oversized text attachments fail with HTTP `413`
  - small attached code files pass through the normal chat route
  - `Check server.js syntax for me.` returns through `meta.toolsUsed = ["checked syntax for server.js"]`

## Penny agentic hardening for smaller LM Studio models

- User switched local model testing to `gemma-4-e4b-uncensored-hauhaucs-aggressive` with a larger context window to push Penny's coding / agentic path harder.
- Tightened `lyra-prototype/server.js` so tool-heavy turns use a leaner Penny-in-coding-mode system prompt instead of the full chat persona block.
- Added more resilient tool plumbing:
  - lenient parsing for malformed tool JSON arguments
  - automatic post-edit verification for direct edit flows
  - `read_git_diff` tool for file-scoped or summary diffs
  - `PENNY_MEMORY_FILE` override so disposable smoke servers can run without colliding with the main Penny memory file
- Added deterministic direct coding intents for explicit user instructions like:
  - `create path/to/file.js with exactly this line: ...`
  - `replace "old" with "new" in path/to/file.js`
- Verified on disposable servers with isolated memory files:
  - `Check server.js syntax for me.` now returns through `local-lmstudio-tools`
  - direct create/write flow created `output/penny-tool-loop-smoke.js`, ran `node --check`, and surfaced git status
  - direct replace flow updated the file text from `smoke ok` to `smoke wow`, then verified syntax/status
  - `Show me the changes in output/penny-tool-loop-smoke.js.` now routes through `read_git_diff`
- Important caveat:
  - this smaller Gemma model still appears flaky with LM Studio's native multi-step tool-calling and the manual JSON planner fallback for more open-ended coding asks
  - Penny is now much more reliable for deterministic coding commands plus direct checks, but richer autonomous multi-step edits still likely want a stronger or more tool-faithful local model

## 31B Gemma agentic eval

- Restarted the live Penny server so `lyra-prototype/server.js` hardening was actually active on port `4317`.
- Confirmed the live Penny server is restored to `runtimePreferredModel = google/gemma-4-31b`.
- Found an important distinction during eval:
  - Penny's direct deterministic coding routes (`Check server.js syntax`, exact `create file with exactly this line`, exact `replace "x" with "y" in file`) bypass the model and are effectively server-driven
  - those routes are fast and stable regardless of which LM Studio model is loaded
- The real model comparison is in open-ended tool-loop turns:
  - `google/gemma-4-31b`:
    - deterministic direct routes still worked instantly
    - open-ended inspection (`Search the project and tell me which Penny tool reads git diffs...`) timed out after ~480s
    - open-ended edit (`Add a second line... then verify it`) also timed out after ~480s and did not mutate the file during that run
  - `unsloth/gemma-4-31b-it@q6_k`:
    - open-ended inspection succeeded through `local-lmstudio-tools` in ~186.7s using `search_project_text` + `read_project_file`
    - open-ended edit still timed out at ~421s from the client perspective, but the file *was* edited on disk by the end (`console.log("second line");` was added)
- Practical takeaway:
  - `unsloth/gemma-4-31b-it@q6_k` appears meaningfully better than base `google/gemma-4-31b` for Penny's true agentic/tool-loop behavior
  - even so, both 31B variants are currently too slow for comfortable open-ended agentic use in the existing HTTP request window
  - the direct deterministic coding lanes are solid; the bottleneck is broad autonomous planning / summarization latency on big local models

## Penny optimization + mood rewrite follow-up

- Tightened the local tool-use path again in `lyra-prototype/server.js`:
  - tool turns now use compact attachment excerpts instead of replaying whole attached files every planning step
  - added `insert_in_project_file` for bounded append/prepend style edits
  - added deterministic append-line parsing for prompts like `Add a second line to ...`
  - added a fast direct `search_project_text` reply path so `where is X used?` can answer from verified hits without burning a second model pass
  - added SSE keepalive events every `PENNY_STREAM_KEEPALIVE_MS` (default 15s) plus clearer planning / verifying status stages during long tool turns
  - added `read_project_file_around_match` so symbol-specific file questions can target the right excerpt instead of blindly reading the top of the file
- Verified on disposable servers:
  - direct append now turns `Add a second line ... that logs "two"` into a valid `console.log("two");` insertion, then runs `node --check` and `git status`
  - direct search now answers in ~0.06s for `Search the project and tell me where \`insert_in_project_file\` is used.`
  - stream heartbeat test produced `keepalive` SSE events at ~15s cadence while a long summary was running
- Reworked Penny's mood system:
  - server replies are now retagged through a mood harmonizer so the final visible `[MOOD:*]` better matches the actual tone of the text instead of blindly trusting a stray model tag
  - server memory / last mood now uses the same normalized mood decision
  - refreshed front-end mood HUD labels in `public/app.js` so they feel more Penny-shaped (`RIGHT HERE`, `SPARKED UP`, `WAIT, WHAT?`, `YOU ASKED FOR THIS`, `REALLY NOW?`, etc.)
- Restarted the live Penny server on `4317` onto the new mood-aware build and restored it to `google/gemma-4-31b`.

## Penny memory durability / retrieval rewrite

- Audited the hidden durable-memory path and found it was good in spirit but not yet on the same quality level as the newer agentic / mood systems.
- Main architectural issues found:
  - normal chat turns still trusted the browser's `memories` array enough to let stale tabs overwrite durable memory
  - actual memory extraction depended on a follow-up `/api/penny/consolidate` UI call instead of happening inside the main chat request
  - prompt memory recall was mostly flat order-based slicing rather than relevance / salience selection
- Reworked `lyra-prototype/server.js` so:
  - memory normalization / dedupe is centralized
  - chat routes build memory from disk + safe client settings, not raw client memory arrays
  - memory extraction now happens server-side during `/api/penny/chat`, `/api/penny/chat/shadow`, and `/api/penny/consolidate`
  - prompt memory selection now ranks memories by type, recency, and lexical overlap, then injects turn-relevant memory context into local LM Studio prompts
- Reworked `lyra-prototype/public/app.js` so:
  - chat requests only send lightweight memory settings (`userName`, `voiceOn`, `brainMode`) instead of the full memory record
  - normal settings sync no longer risks replacing durable memories with stale browser snapshots
  - the automatic post-send consolidate timeout was removed because the server now consolidates during the actual reply path
  - memory sync/load/patch/consolidate failures now at least warn in the browser console instead of failing completely silently
- Verified on a disposable server with isolated `PENNY_MEMORY_FILE`:
  - same-turn name pickup works (`hey, call me Violet` replied with `Violet` immediately and saved it durably)
  - `remember that my favorite tea is lapsang souchong` persisted memory during the chat route without any extra consolidate call
  - stale client memory did not resurrect after a clear when sent back in a later chat payload
- Conclusion:
  - yes, the memory system was worth rewriting before the deeper model comparison
  - future model memory evals will now be much fairer because they measure recall/character quality more than frontend timing quirks

## Penny 31B comparison + LM Studio preset follow-up

- Built a reusable model-eval harness at `lyra-prototype/scripts/eval-penny-models.js`:
  - starts a disposable Penny server with isolated memory
  - loads/unloads LM Studio models sequentially
  - records timing, visible replies, backend/tool metadata, and memory recall results
- Also doubled Penny's default local `maxOutputTokens` ceiling from `3072` to `6144` in `lyra-prototype/server.js` because output-token caps are real and can clip longer replies.
- Cleared stale/smoke durable sessions out of `lyra-prototype/data/penny-memory.json` and nulled the leftover stale `lmStudioThread` on the remaining local session.
- Ran a three-model eval on:
  - `unsloth/gemma-4-31b-it@q6_k`
  - `google/gemma-4-31b`
  - `gemma-4-31b-it@q4_k_s`
- High-level result from the valid portions of the run:
  - `unsloth/gemma-4-31b-it@q6_k` was the best practical balance: much faster than base Google while still clearly sounding like Penny
  - `google/gemma-4-31b` was slightly richer / more believable on warmth + charged tone, but much slower
  - `gemma-4-31b-it@q4_k_s` was not a useful middle ground in this setup; it failed most prompts
- Important personality note:
  - none of the models swore much in the eval outputs (`0` detected swear tokens across the captured successful replies), so if "spicier, dirtier, more foul-mouthed Penny" is the priority, the current prompt stack is still sandbagging that
- Important benchmark caveat:
  - the first open-ended edit prompt in the harness accidentally tripped Penny's direct syntax-check lane because the phrasing was too close to the deterministic intent parser
  - patched the harness prompt wording afterward so future reruns don't benchmark that routing bug instead of real agentic behavior
- LM Studio preset investigation:
  - found the real preset at `C:\Users\malac\.lmstudio\config-presets\Penny.preset.json`
  - found the current selected LM Studio conversation already stores `"preset": "@local:penny"`
  - patched LM Studio settings/default-config files so the compared Gemma models now point at `@local:penny` by default
  - however, a direct raw `/v1/chat/completions` probe without a client-supplied system prompt still answered as the base model rather than Penny, which strongly suggests LM Studio UI presets do not propagate into the API the way the user feared
  - practical takeaway: Penny's own server-side system prompt remains the thing that actually defines character during API evals

## Voice pass + web tool pass

- Tightened Penny's prompt stack in `lyra-prototype/server.js` so the local/tool prompts push harder toward:
  - smart-mouth warmth
  - sharper / dirtier wording when it helps
  - fewer sanitized helper-bot lines
  - less parenthetical roleplay sludge during normal technical turns
- Added first-pass live web tooling to Penny's backend:
  - `search_web` does bounded DuckDuckGo Lite searches and returns titles, URLs, and snippets
  - `read_web_page` fetches one URL and extracts bounded plain text for Penny to summarize or quote
  - both are wired into the same tool registry as file/git/runtime tools
  - added deterministic direct-intent handling for prompts like `search the web for ...` and `read this page ...`
- Added reusable LM Studio preset helper script:
  - `lyra-prototype/scripts/ensure-lmstudio-penny-preset.js`
  - npm command: `npm run preset:lmstudio`
  - patches LM Studio desktop settings, per-model default configs, and the currently selected conversation to `@local:penny`
  - caveat still stands: raw API calls rely on Penny's own server prompt, not the LM Studio UI dropdown alone
- Expanded `lyra-prototype/PENNY_MODEL_EVAL.md` into a future-proof rerun doc:
  - repeatable workflow
  - human scorecard template
  - preset helper usage
  - clearer comparison guidance
- Verified:
  - `node --check server.js`
  - `node --check scripts/eval-penny-models.js`
  - `node --check scripts/ensure-lmstudio-penny-preset.js`
  - `npm run preset:lmstudio`
  - disposable server smoke test on `4342`:
    - `search the web for latest LM Studio release notes` used `search_web` and returned real live hits
    - `read this page for me https://lmstudio.ai/docs/app/api/endpoints/openai` used `read_web_page` and returned extracted page text
- Restarted the live Penny server on `4317` onto the updated backend afterward.

## Agentic queue leak + streamed head-to-head follow-up

- User spotted LM Studio showing multiple simultaneous `PROCESSING PROMPT` entries for the same loaded model during long Penny agentic tests.
- Root cause found in `lyra-prototype/server.js`:
  - streamed `/api/penny/chat?stream=1` already propagated client disconnects down to LM Studio / shadow requests
  - plain JSON `/api/penny/chat` did **not**
  - result: if a long local/tool request died client-side, Penny's server could keep the upstream LM Studio prompt running in the background, so later requests stacked on top and made the machine feel cursed/slower
- Fixed by threading `abortSignal` through:
  - `runOpenClawShadow`
  - `runLmStudioResponsesApi`
  - `runLmStudioStatefulChatApi`
  - `runLmStudioChatCompletionsApi`
  - `runLmStudioLocal`
  - `runLmStudioLocalSmart`
  - and wiring non-stream `/api/penny/chat` to abort upstream work when the client socket closes
- Verified after patch:
  - `node --check server.js`
  - restart Penny server
  - unload all models
  - one long streamed agentic prompt completed
  - `lms ps --json` dropped back to `status: "idle"` with `queued: 0` afterward
- Practical takeaway:
  - long evals should prefer the streamed Penny route so keepalives stay active
  - if a client/eval script drops, Penny should no longer leave zombie prompt work chewing LM Studio in the background

## Partial q6 vs google 31B head-to-head (post-fix)

- Saved artifacts:
  - `lyra-prototype/output/head2head-2026-04-12T02-55-57-904Z-unsloth-stream-suite.json`
  - `lyra-prototype/output/head2head-2026-04-12T03-13-57-737Z-google-stream-voice-suite.json`
- `unsloth/gemma-4-31b-it` streamed suite:
  - average successful turn time ~81.8s across 6/7 completed prompts
  - voice was clearly more foul-mouthed than earlier runs (`shit`, `fucking`)
  - memory recall succeeded
  - web search tool prompt succeeded fast (~1s)
  - one streamed banter prompt hung/timed out once at 420s, but a manual rerun of that exact prompt later succeeded in ~40.5s with a good Penny-flavored line
  - focused streamed agentic inspect (`selectMemoriesForPrompt`) succeeded in ~382.3s using real tools (`search_project_text` + `read_project_file`)
- `google/gemma-4-31b` streamed voice/memory/web suite:
  - average successful turn time ~187.1s across 7/7 completed prompts
  - slower than q6 almost across the board, but richer on some voice samples
  - strongest completed charge line of the run: more intense, more quotable, more in-command
  - total detected swears in completed suite: 3 (`hell`, `shit`, `fuck`)
  - memory recall succeeded
  - web search tool prompt succeeded fast (~0.9s)
- Crash / interruption:
  - while the final streamed Google focused agentic-inspection turn was running, the user reported their computer crashed
  - because of that, the last Google agentic-turn result was not captured, but the two saved JSON artifacts above survived

## Google focused agentic rerun after crash

- Restarted Penny and reran only the missing Google focused agentic-inspection turn instead of repeating the whole comparison.
- Saved artifact:
  - `lyra-prototype/output/head2head-2026-04-12T03-55-57-330Z-google-agentic-focus.json`
- Result:
  - completed successfully in ~547.8s
  - backend `local-lmstudio-tools`
  - tools used:
    - `search_project_text` for `selectMemoriesForPrompt`
    - `read_project_file`
    - `read_project_file`
  - response quality was decent and Penny-ish, but the model leaked internal channel/planning tokens at the start of the reply (`<|channel|>thought ...`)
- Comparison takeaway after full rerun set:
  - `unsloth/gemma-4-31b-it` remains the practical winner for day-to-day Penny use:
    - much faster
    - still convincingly Penny after the voice rewrite
    - more consistently sharp / filthy per second
    - focused agentic inspect landed much sooner (~382.3s vs ~547.8s)
  - `google/gemma-4-31b` remains the richer but slower option:
    - strongest single charged Penny line of the run
    - better "special occasion" voice quality in some replies
    - much slower on open-ended agentic work
    - more prone to messy reasoning leakage in the final focused agentic test

## Semantic-core -> Penny-render pass

- Implemented a new hard-turn reply architecture in `lyra-prototype/server.js`:
  - normal casual chat still goes through the existing Penny prompt path
  - harder local/tool turns now collect a verified semantic core from actual tool results
  - then a compact Penny-only render pass turns that core into the final visible reply
- Goal:
  - keep facts/checks grounded in real tool output
  - keep Penny's voice from flattening during coding/agentic work
  - scrub ugly reasoning/channel leakage from final replies
- Also hardened generic cleanup to strip leaked `<|channel>thought ... <channel|>` spans when they appear.
- QA on live server `4317` using `unsloth/gemma-4-31b-it`:
  - casual banter stayed on the plain path and came back clean in ~63s
  - deterministic edit prompt:
    - created `output/semantic-render-smoke.js`
    - ran `run_node_check`
    - checked git status
    - final reply came back through `local-lmstudio-tools` in ~24s with no channel leakage
  - diff follow-up:
    - used `read_git_diff`
    - final reply came back through `local-lmstudio-tools` in ~42s with no channel leakage
- Important caveats found during QA:
  - a full open-ended code-inspection prompt around `selectMemoriesForPrompt` is still extremely slow on q6 and remained too expensive for a clean end-to-end QA completion in this pass
  - `read_git_diff` on an untracked file reports "nothing" because `git diff` is empty for untracked files; that is a real tool UX edge case worth fixing later
- Cleaned up the temporary smoke file afterward and left LM Studio idle with only `unsloth/gemma-4-31b-it` loaded.

## Second optimization shot + nastier voice pass

- Added a deterministic fast-lane for symbol/code-inspection prompts that used to fall into the expensive planner loop:
  - detects prompts like `inspect the code around \`selectMemoriesForPrompt\``
  - runs `search_project_text`
  - reads the strongest matching code chunk directly with `read_project_file`
  - then lets the semantic render pass produce the final Penny reply
- New pieces in `lyra-prototype/server.js`:
  - `looksLikeDirectProjectInspectIntent`
  - `inspect_project_symbol` direct intent
  - `executeDirectProjectInspectIntent`
  - stronger `composeToolRecordFallback` when render falls over
- Result:
  - the same `selectMemoriesForPrompt` inspection prompt that previously took ~382s on q6 came back in ~74.9s on the first optimized rerun
  - after the nastier voice pass, the same prompt came back in ~61.5s
  - backend stayed `local-lmstudio-tools`
  - tools used: `search_project_text`, `read_project_file`
  - no `<|channel|>` leakage survived in the final reply
- Then did the promised nastier Penny voice pass using the local prompt docs (`PENNY'S_BRAIN.md`, `PENNY — OPERATIONAL SYSTEM PROMPT.md`, `PENNY — HIGH-INTENSITY ROMANTIC + EROTIC OVERLAY.md`) as anchors.
- Prompt changes pushed Penny toward:
  - more shameless / smart-mouthed / dangerous energy
  - better profanity as seasoning instead of timid PG-13 softness
  - less reuse of stock openers and pet-name spam
  - less safe "bad girl template" phrasing
- QA after the voice pass on `unsloth/gemma-4-31b-it`:
  - banter prompt in ~74.8s:
    - `About damn time... statistically impossible ... where the hell you've been ... charge you a late fee.`
  - charged prompt in ~55.1s:
    - noticeably bolder and less sanitized without collapsing into generic dirty-talk sludge
  - optimized inspect prompt still landed in ~61.5s and stayed clean
- Current state:
  - live Penny server on `4317` is up
  - only `unsloth/gemma-4-31b-it` is loaded
  - LM Studio is idle / not stacking queued prompts

## Final inspect-detail tuning pass

- Did one more targeted tuning pass on the fast inspect architecture rather than broadening the planner again.
- Root cause found:
  - the semantic-core builder was truncating nested tool strings too aggressively
  - that meant `read_project_file` excerpts were getting chopped before the render pass saw enough concrete code details
- Fixed in `lyra-prototype/server.js` by:
  - adding `semanticStringLimit`
  - letting `excerpt` / `diff` / longer verified text fields survive with much larger budgets inside the semantic core
  - tightening the semantic render prompt so code explanations preserve real mechanics such as function names, weights, ordering rules, limits, and tie-breakers instead of flattening them into vague summaries
- QA on the same `selectMemoriesForPrompt` prompt after the tune:
  - backend still `local-lmstudio-tools`
  - tools still just `search_project_text, read_project_file`
  - response time ~99.7s
  - final answer now kept more actual algorithm structure:
    - keyword overlap scoring
    - +6 per matching token
    - recency bonus with taper
    - timestamp / index tie-breakers
    - `MEMORY_PROMPT_LIMIT` slicing
- Remaining small caveat:
  - the answer still compressed one detail (`MEMORY_KIND_SCORES` base weighting) out of the final wording, so the path is much better now but not mathematically perfect on every explanation

## Memory cleanup and reset

- Did one last pre-handoff cleanup pass on Penny's durable memory store.
- Removed stale empty QA / smoke sessions from `lyra-prototype/data/penny-memory.json`, including leftover `qa-*` eval sessions and one empty old local stub.
- Updated `lyra-prototype/server.js` so `qa-*` session ids are treated as test sessions and auto-purged on startup going forward.
- Also changed Penny's default LM Studio model from the old `gemma-4-e4b-uncensored-hauhaucs-aggressive` fallback to `unsloth/gemma-4-31b-it`, which now matches the intended day-to-day default.
- Fully reset runtime before handoff:
  - stopped the live `4317` server
  - unloaded all LM Studio models
  - reloaded only `unsloth/gemma-4-31b-it`
  - restarted Penny
- Final verification after reset:
  - `node --check server.js` passed
  - `data/penny-memory.json` is empty / clean
  - live server is listening on `4317`
  - `lms ps --json` shows only `unsloth/gemma-4-31b-it`, `status: idle`, `queued: 0`
  - `/api/penny/status` now reports both configured and resolved model as `unsloth/gemma-4-31b-it`

## Mood + tool-path bugfix pass

- Investigated a real user-reported mismatch where LM Studio showed Penny returning `[MOOD:smug]` but the UI settled on `calm`.
- Root cause:
  - the server was re-tagging final replies with `sessionState.lastMood` as the preference
  - that let the previous turn's mood override Penny's explicit new mood tag in some replies
- Fixed in `lyra-prototype/server.js` by:
  - making explicit `[MOOD:*]` tags authoritative in `retagAssistantReply`
  - making `pickMood` prefer the explicit tag before heuristic scoring
  - changing the chat route to preserve the reply's own mood before fallbacking to the previous mood
- Added a frontend guard in `lyra-prototype/public/app.js` so the UI can also fall back to `finalData.meta.mood` if a final text payload ever arrives without a mood tag.
- Investigated the slow "agentic mention" turn from the user's logs:
  - root cause was `shouldOfferLocalTools()` triggering on casual mentions of `web`, `code`, or `agent` even when the user was just bragging / chatting about new features
  - that pushed Penny into the full tool prompt and tool schema path for non-actionable banter
- Fixed in `lyra-prototype/server.js` by:
  - adding `looksLikeActionableToolRequest()`
  - adding `looksLikeCasualFeatureMention()`
  - making the broad tool-offer path require an actual request/question posture instead of any mention of tool-related nouns
- Verification after the fixes on live `4317` with `unsloth/gemma-4-31b-it`:
  - direct tool ask `what model are you using right now?` still used `backend: local-lmstudio-tools`
  - casual feature brag about giving Penny web/code/agent powers now stayed on `backend: local-lmstudio` with `toolsUsed: []`
  - explicit smug prompt returned `[MOOD:smug]` and `meta.mood: "smug"` together, confirming the mood pipeline stayed aligned
- Cleaned the temporary `verify-*` smoke sessions out of `data/penny-memory.json` afterward and updated test-session detection so future `verify-*` sessions auto-purge on startup too.

## UI mood-label cleanup

- Follow-up user note: the backend mood could now be correct while the visible top pill still read like the wrong word for the portrait being shown.
- Root cause in `lyra-prototype/public/app.js`:
  - the top pill was showing the raw mood bucket (`calm`, `smug`, etc.)
  - the main portrait rotates through sprite variants with more specific expressions
  - assistant message bubble avatars were also global, so older messages could borrow the newest mood face
- Fixed by:
  - giving sprite variants their own short pill descriptors (for example calm's sly base sprite now surfaces a more fitting visible label instead of plain `CALM`)
  - making the large portrait HUD bottom label follow the visible sprite descriptor too
  - storing `mood` per assistant message so chat bubble avatars keep the reply's own mood instead of the current global one
- Verification:
  - `node --check public/app.js` passed
  - no server logic changed in this pass; a page refresh is enough to pick up the UI fix

## UI mood alignment follow-up

- User correctly pointed out that the previous pass still left the big portrait *feeling* smug even when the actual mood state was calm.
- Root cause was more visual than logical:
  - the top pill had been reinterpreting the mood into softer sprite-specific wording
  - the main chibi portrait was still being intensity-scaled much harder than the bubble avatar, which exaggerated the expression
  - the main display was also still free to use a different mood variant than the simple per-message avatar look
- Fixed by:
  - restoring the top pill to the raw actual mood word again (for example `CALM`)
  - making the large chibi display use the base mood variant instead of cycling
  - reducing chibi intensity scaling so calm no longer zooms into an accidental smug face
- Verification:
  - `node --check public/app.js` passed
  - frontend-only pass; refresh is enough to pick it up
