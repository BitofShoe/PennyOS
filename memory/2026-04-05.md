# 2026-04-05

- First real identity-setting conversation.
- User asked me to make myself as close as possible to the CODE27 / Codie companion concept.
- Initial working identity was Codie: digital companion, house familiar in the machine, warm and capable, more companion than servant.
- Then user directed me to use `LYRA_OPENCLAW_COMPANION.md` as the implementation brief.
- Identity refined to **Lyra**.
- Desired qualities: warm, sharp, observant, emotionally intelligent, subtly witty, premium companion-core presence.
- Keep the companion vibe, but avoid overwriting unrelated setup.
- Future work if desired: voice IO, frontend vessel/avatar, ambient presence states, backend route for companion-specific UI.
- Built initial local vessel prototype in `lyra-prototype/`: premium companion-core UI, animated face/halo, mood-aware theming, local `/api/companion/chat` and `/api/companion/status` routes, no external npm dependencies.
- Tested the prototype locally at `http://localhost:4317`.
- Important lesson: the first backend was intentionally a placeholder stub for UI testing only, so it feels noticeably dumber/glitchier than the real assistant. User noticed immediately and correctly. Next major step is replacing the stub brain with a real model/OpenClaw-backed intelligence layer.
- Troubleshot a failure where the prototype stopped working because the server had been launched through a timed background exec and got reaped. Added durable launcher scripts `start-lyra.ps1` and `stop-lyra.ps1` so the local server stays up reliably.
