# 2026-04-19

- Operator preference update: prefer WSL-style shell commands when practical. If a chat thread or tool metadata still reports `powershell`, treat that as a runtime/tooling mismatch and say so plainly instead of assuming PowerShell is the user's preferred shell.
