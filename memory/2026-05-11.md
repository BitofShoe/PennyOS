# 2026-05-11

- Goal mode was active for the Penny local-LLM sidecar roadmap follow-through.
- Sidecar work tightened Pi template support against local Pi 0.74.0 docs/schema, added safe Pi template validation and copy-plan output, broadened endpoint compatibility probes for explicit model-call mode, added a license/access review queue, validated example configs/fixtures in tests, refreshed sidecar proof artifacts, and preserved Penny guardrails: sidecars remain sidecars, no memory ingest, no default model/runtime/prompt/context changes, no live model compare, no public/home/camera/social automation.
- Verification receipts: targeted sidecar tests passed, full `npm test` passed with 894 tests / 891 pass / 3 todo, and `git diff --check` passed.
- Live checks: Pi present at `/home/malac/.nvm/versions/node/v24.15.0/bin/pi` with version `0.74.0`; `~/.pi/agent/models.json` absent; OpenCode not on PATH; WSL `http://127.0.0.1:1234/v1` endpoint unavailable from the read-only `/v1/models` probe; Qwen-vs-Gemma compare remains prepared-only.
- User pushed back that the first Goal-mode completion was too small for the stated bar. Correction: do not treat guardrails as the finish line. I added a second pass with raw Pi `models.json` generation, endpoint-artifact model-id resolution, direct npm aliases (`penny:pi:models-json`, `penny:pi:validate-template`, `penny:pi:copy-plan`, `penny:apps:license-review`, `penny:endpoint:probe:model-call`), stronger trial command previews, docs, and tests. Full `npm test` after the second pass: 897 tests / 894 pass / 3 todo; `git diff --check` passed.
