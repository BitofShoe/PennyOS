# 2026-03-10


## Durable Notes (memory flush)
- User resumed active LitBits/Sora production work and confirmed paid Sora access is now active (25s videos + storyboard mode availability).
- User asked whether LitBits can use Sora storyboard mode by default; guidance given to set storyboard-first workflow with fallback to single-prompt mode.
- Recommended consistent structure with variation matrix (same storyboard skeleton, varied setting/camera/tone/metaphor/audio) to avoid repetitive outputs.
- For DROP 8 (Building Credit), user proceeded with ASK-mode workflow and received partial submit due to Sora concurrency cap (3 jobs max at a time).
- Operational pattern validated again: small safe batches, explicit resume commands, and verification sweeps for IDs.
- User encountered OpenClaw update banner issue; root cause was non-git install update path. Ran manual update command:
  - npm i -g openclaw@latest
  - openclaw doctor
- Doctor reported warnings worth remembering:
  - Telegram groupPolicy=allowlist with empty allow lists (group messages dropped unless configured)
  - Gateway service embeds token; suggested reinstall with `openclaw gateway install --force`
  - Memory semantic search not fully ready due missing embedding provider/config and a module import error in current install.
- Gateway restart attempt returned transcript-repair synthetic error message; follow-up service state check should be done before next automation run.
