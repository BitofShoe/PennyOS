# 2026-04-22

- Open-loop tracker Slice O1 landed as a pure schema/model slice only. Added `lib/penny-open-loops.js` and `test/penny-open-loops.test.js` with `penny-open-loop-state.v1`, status constants, `normalizeOpenLoop`, `normalizeOpenLoopState`, `summarizeOpenLoopState`, and `classifyOpenLoopStatus`.
- Landed guardrails: open-loop authority normalizes to advisory; missing id/title/status is rejected; completed, dismissed, expired, `manual-only`, and zero-surface-count loops are held back from surfaceable summaries; stale expiry is classified without mutating stored status.
- Deferred: O2 relevance scoring, O3 local store, O4 fixture extraction, O5 prompt bridge fixture, O6 live prompt bridge, O7 lifecycle operations, O8 compare harness, O9 docs, and any runtime voice/server/PromptTruth/toolEvidenceReceipt/prompt-limit/initiative behavior changes.
- Verification before journal update: `node --test test/penny-open-loops.test.js` passed (`6 passing`), `node --check lib/penny-open-loops.js` passed, and `git diff --check` was clean.
- Open-loop tracker Slice O2 landed as pure relevance scoring only. Added `selectRelevantOpenLoops` in `lib/penny-open-loops.js` with inspectable `selected`/`heldBack` output, explicit-anchor scoring, token overlap, recency, priority, project/thread hints, static-candidate relation, advisory prompt snippets, default `maxLoops: 1`, and suppression for completed/dismissed/expired/manual-only/zero-surface loops.
- O2 guardrails preserved: no storage, no live prompt bridge, no runtime wiring, no `server.js`, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no initiative policy, and no automatic explicit-memory writes.
- O2 verification before journal update: `node --test test/penny-open-loops.test.js` passed (`10 passing`), `node --check lib/penny-open-loops.js` passed, `node --check test/penny-open-loops.test.js` passed, and `git diff --check` was clean.
- Open-loop tracker Slice O3 landed as storage/disposable-QA state only. Added `lib/penny-open-loop-store.js` and `test/penny-open-loop-store.test.js` with local `PENNY_OPEN_LOOP_FILE` path resolution, atomic JSON writes through the shared atomic writer, normalized `penny-open-loop-state.v1` reads/writes, explicit corrupt-file fallback artifacts, and disposable open-loop state config for QA.
- O3 also wired existing server-spawning QA/eval scripts to pass disposable `PENNY_OPEN_LOOP_FILE` values and added `data/penny-open-loops*.json` to `.gitignore`; this prepares QA isolation without live prompt/runtime wiring.
- O3 guardrails preserved: no extraction, no live prompt bridge, no `server.js` runtime wiring, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no initiative behavior, no lifecycle operations, and no automatic explicit-memory writes.
- O3 verification before journal update: `node --test test/penny-open-loop-store.test.js test/penny-open-loops.test.js test/penny-memory-qa-script.test.js test/penny-runtime-fit-script.test.js` passed (`38 passing`), post-cleanup focused `node --test test/penny-voice-redo.test.js test/penny-memory-qa-script.test.js test/penny-runtime-fit-script.test.js test/penny-open-loop-store.test.js` passed (`48 passing`), full `npm test` passed (`520 passing, 0 failing, 3 todo`), changed-owner `node --check` passed, and `git diff --check` was clean.
- Open-loop tracker Slice O4 landed as fixture extraction only. Added `lib/penny-open-loop-extraction.js` and `test/penny-open-loop-extraction.test.js` with `penny-open-loop-suggestions.v1`, controlled session/reflection artifact parsing, advisory normalized `openLoopSuggestions`, source/excerpt metadata, confidence/status/next-step extraction, completed/deferred/in-progress classification, sensitive-private-inference rejection, and explicit speculation labels.
- O4 guardrails preserved: suggestions are not written to the open-loop store, no live prompt bridge, no `server.js`, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no initiative behavior, no lifecycle operations, and no automatic explicit-memory writes.
- O4 verification before journal update: `node --test test/penny-open-loop-extraction.test.js` passed (`5 passing`), focused O1-O4 `node --test test/penny-open-loop-extraction.test.js test/penny-open-loops.test.js test/penny-open-loop-store.test.js` passed (`21 passing`), changed-owner `node --check` passed, full `npm test` passed (`525 passing, 0 failing, 3 todo`), and `git diff --check` was clean.
- Open-loop tracker Slice O5 landed as fixture-only prompt bridge work. Added `penny-open-loop-prompt-bridge.v1` helpers in `lib/penny-open-loops.js`, including compact advisory bridge snippets, max-one rendered loop, 40-120 word cap enforcement, source/relevance labels, selected-vs-held-back fixture output, and explicit no-overclaim/no-canonical-memory guardrails. Added `scripts/eval-penny-open-loop-bridge.js` and `test/penny-open-loop-bridge.test.js`; expanded `test/penny-open-loops.test.js`.
- O5 fixture runner wrote ignored artifact `output/open-loop-bridge-fixture-2026-04-22T03-36-29-118Z.json` with 3 passing cases, 2 rendered snippets, 3 held-back loops, selected-vs-held-back coverage, and overclaim guardrails present.
- O5 guardrails preserved: no live prompt bridge, no `server.js`, no runtime voice changes, no PromptTruth expansion or new PromptTruth channel, no `toolEvidenceReceipt` changes, no prompt-limit increase, no initiative behavior, no lifecycle operations, no open-loop store writes, and no automatic explicit-memory writes.
- O5 verification before journal update: `node scripts/eval-penny-open-loop-bridge.js` passed and wrote the fixture artifact; focused `node --test test/penny-open-loop-bridge.test.js test/penny-open-loop-extraction.test.js test/penny-open-loops.test.js test/penny-open-loop-store.test.js` passed (`27 passing`); changed-owner `node --check` passed; full `npm test` passed (`531 passing, 0 failing, 3 todo`); and `git diff --check HEAD` was clean.
- Open-loop tracker Slice O8 landed as a mock-route QA/compare harness. Added `scripts/eval-penny-open-loop-compare.js`, `test/penny-open-loop-compare.test.js`, and `npm run eval:open-loop-compare`.
- O8 compares `open-loop-off` vs `open-loop-on` through disposable Penny servers and a mock LM Studio backend with seeded advisory/terminal open-loop state. It scores continuity wins, adjacent-topic bleed, annoyance regressions, overclaim regressions, prompt token delta, mock latency delta, and an enablement recommendation.
- Fresh O8 artifact: `output/open-loop-compare-mock-2026-04-22T04-13-07-858Z.json`. Result: `pairedVerdict: open-loop-on`, `enablementRecommendation: eligible-for-opt-in`, `totalDelta: 9`, `continuityWins: 3`, `adjacentTopicBleed: 0`, `annoyanceRegressions: 0`, `overclaimRegressions: 0`, `promptTokenDelta: 332`, `totalLatencyDelta: -20`, and `trustVerdict: pass`.
- O8 guardrails preserved: no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` merge, no prompt-limit increase, no automatic explicit-memory writes, no user memory touched, and no live LM Studio/model load behavior.
- O8 verification before journal update: changed-owner `node --check` passed; `node --test test/penny-open-loop-compare.test.js` passed (`7 passing`); `npm run eval:open-loop-compare` passed and wrote the artifact above; focused adjacent coverage `node --test test/penny-open-loop-compare.test.js test/penny-open-loop-bridge.test.js test/penny-open-loops.test.js test/penny-open-loop-store.test.js test/penny-memory.test.js` passed (`62 passing`); full `npm test` passed (`548 passing, 0 failing, 3 todo`); and `git diff --check HEAD` was clean.
- Next natural open-loop slice is O9 docs: explain open loops as advisory, dismissible, expirable continuity state and document that O8 only made the bridge eligible for opt-in evidence, not default law.
- Open-loop tracker Slice O9 landed as docs-only current-law cleanup. Updated `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md` to describe open loops as advisory unresolved-thread continuity, separate from explicit memory, with source refs, expiry, lifecycle history, dismissal/completion/defer controls, and the `PENNY_OPEN_LOOP_FILE` store seam.
- O9 also documented that `PENNY_ENABLE_OPEN_LOOP_PROMPT=1` is still an opt-in live bridge, capped by `PENNY_OPEN_LOOP_MAX_RENDERED` and `PENNY_OPEN_LOOP_MAX_TOKENS`, and that the O8 compare result means eligible for local opt-in evidence rather than default runtime law.
- O9 guardrails preserved: no runtime behavior change, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` merge, no prompt-limit increase, no automatic explicit-memory writes, no autonomous completion/execution, and no open-loop plan sprawl beyond Plan 2 docs.
- O9 verification before journal update: focused open-loop adjacent coverage `node --test test/penny-open-loops.test.js test/penny-open-loop-store.test.js test/penny-open-loop-bridge.test.js test/penny-open-loop-compare.test.js test/penny-memory.test.js` passed (`62 passing`); full `npm test` passed (`548 passing, 0 failing, 3 todo`); and `git diff --check HEAD` was clean.
- Plan 2 open-loop tracker is now documented through O9; future work should treat the live prompt bridge as bounded local opt-in only unless a later measured slice explicitly changes default law.
- Bounded initiative policy Slice I1 landed as a pure schema/helper slice only. Added `lib/penny-initiative-policy.js` and `test/penny-initiative-policy.test.js` with `penny-initiative-decision.v1`, `INITIATIVE_TYPES`, `decideInitiative`, fixed forbidden actions, high-confidence next-step selection, direct-command suppression, sensitive-topic suppression, recent-initiative cooldown, and explicit stop-suggesting opt-out detection.
- I1 guardrails preserved: no prompt/runtime wiring, no `server.js`, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no autonomous actions, no memory writes, and no scheduled/proactive behavior.
- I1 verification before journal update: `node --test test/penny-initiative-policy.test.js` passed (`5 passing`), `node --check lib/penny-initiative-policy.js` passed, `node --check test/penny-initiative-policy.test.js` passed, and full `npm test` passed (`553 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I2 landed as a pure risk/permission matrix extension to `lib/penny-initiative-policy.js` and `test/penny-initiative-policy.test.js`. Added exported `INITIATIVE_RISK_CLASSES`, candidate/context risk normalization, low/medium/high/blocked gating, medium-risk memory approval gating, high-risk domain-request gating, blocked-risk suppression, side-effect-completion-claim suppression, and unapproved memory-write suppression.
- I2 guardrails preserved: no prompt/runtime bridge, no `server.js`, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no autonomous actions, no memory writes, no scheduling/proactive behavior, and no side-effect claims treated as completed actions.
- I2 verification before journal update: `node --check lib/penny-initiative-policy.js` passed, `node --check test/penny-initiative-policy.test.js` passed, `node --test test/penny-initiative-policy.test.js` passed (`12 passing`), `git diff --check HEAD` was clean, and full `npm test` passed (`560 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I3 landed as a pure optional-input extension to `lib/penny-initiative-policy.js` and `test/penny-initiative-policy.test.js`. The helper now accepts turn-state response mode/user intent/energy, selected open-loop shapes, static memory reflex top candidates, source/trust flags, and urgency/source-check context without importing those subsystems or requiring them.
- I3 policy behavior added: brainstorm/ideation turns can use one central selected open loop as a next-step suggestion; exact/code/source-backed review suppresses extra initiative unless a source-check warning is needed; urgency plus weak/source-uncertain evidence produces a source-check suggestion instead of over-confirmation; candidate-only static memory hits stay source-check shaped rather than truth-shaped; missing optional inputs degrade to no initiative.
- I3 guardrails preserved: no prompt/runtime bridge, no `server.js`, no runtime voice changes, no PromptTruth or `toolEvidenceReceipt` changes, no prompt-limit increase, no autonomous actions, no memory writes, no scheduling/proactive behavior, and no circular dependency on turn-state/open-loop/static-memory modules.
- I3 verification before journal update: `node --check lib/penny-initiative-policy.js` passed, `node --check test/penny-initiative-policy.test.js` passed, `node --test test/penny-initiative-policy.test.js` passed (`18 passing`), `git diff --check HEAD` was clean, and full `npm test` passed (`566 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I4 landed as fixture-only prompt scaffold work. Added `buildInitiativePromptScaffold` in `lib/penny-initiative-policy.js`, preserving the selected decision source/candidate id and emitting a compact `penny-initiative-prompt-scaffold.v1` instruction with max-one-sentence framing, source awareness, no-action/no-memory guardrails, and explicit `livePromptBridge: false` / `promptTruthExpanded: false` receipts.
- Added `scripts/eval-penny-initiative-fixture.js` with `penny-initiative-fixture.v1` cases for allowed source-backed next step, direct-command holdback, urgency/source-check warning, and blocked auto-write memory suggestion. Fresh ignored artifact: `lyra-prototype/output/initiative-fixture-2026-04-22T04-55-20-018Z.json` with 4/4 passing cases, 2 rendered snippets, 2 held-back initiatives, source-aware rendered snippets, and max one rendered initiative.
- I4 guardrails preserved: no live prompt bridge, no `server.js`, no runtime voice changes, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no autonomous actions, no memory writes, no scheduling/proactive behavior, and no live model calls.
- I4 verification before journal update: changed-owner `node --check` passed, `node --test test/penny-initiative-policy.test.js` passed (`22 passing`), `node scripts/eval-penny-initiative-fixture.js` passed and wrote the fixture artifact above, adjacent focused tests passed (`41 passing`), `git diff --check HEAD` was clean, and full `npm test` passed (`570 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I5 landed as a live one-suggestion prompt bridge behind `PENNY_ENABLE_BOUNDED_INITIATIVE`. Added `penny-initiative-prompt-bridge.v1` via `buildLiveInitiativePromptBridge`, configurable `PENNY_INITIATIVE_MAX_PER_TURN` and `PENNY_INITIATIVE_COOLDOWN_TURNS`, recent-message cooldown extraction, current-turn opt-out/direct-command suppression, and source-aware max-one scaffold rendering.
- I5 runtime wiring stays narrow: `server.js` reads open-loop state only when the open-loop prompt bridge or bounded initiative is enabled, derives at most one relevant open-loop next-step candidate, attaches `initiativePromptBridge` to enriched prompt memory, renders it under `Wake state - optional initiative (advisory)`, and records a compact sibling `modelAdvisory.initiativePromptBridge` trace. PromptTruth and `toolEvidenceReceipt` remain unchanged.
- I5 guardrails preserved: off by default, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` merge/change, no prompt-limit increase, no autonomous actions, no explicit-memory writes, no scheduling/proactive background behavior, no source-verification claims without receipts, and direct answers remain direct via policy suppression.
- I5 fixture runner still passed and wrote ignored artifact `lyra-prototype/output/initiative-fixture-2026-04-22T05-05-50-481Z.json` with 4/4 passing cases, 2 rendered snippets, 2 held-back initiatives, source-aware rendered snippets, and max one rendered initiative.
- I5 verification before journal update: changed-owner `node --check` passed; focused tests passed for `test/penny-initiative-policy.test.js` (`25 passing`), `test/penny-memory.test.js` (`25 passing`), `test/penny-runtime-artifacts.test.js` (`22 passing`), `test/penny-open-loops.test.js` (`15 passing`), `test/penny-route-handlers.test.js` (`10 passing`), and `test/penny-routes.test.js` (`10 passing`); `node scripts/eval-penny-initiative-fixture.js` passed; `git diff --check HEAD` was clean; and full `npm test` passed (`574 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I6 landed as pure user-control and dismissal policy work. Added `penny-initiative-user-controls.v1` via `extractInitiativeUserControls`, with explicit opt-out, current-turn opt-in, thread-watch consent, reminder dismissal parsing, stored preference handling, and dismissal target extraction for selected/open-loop-shaped candidates.
- I6 also added pure `applyOpenLoopDismissals` in `lib/penny-open-loops.js` so a caller can turn explicit dismissal controls into advisory dismissed loop state without store writes by default. Dismissed/completed/expired loops stay terminal-safe, and missing loop ids are reported as held back instead of invented.
- I6 guardrails preserved: no `server.js`, no live runtime storage mutation, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no autonomous actions, no explicit-memory writes, no scheduling/proactive background behavior, and opt-in does not bypass risk gates.
- I6 fixture runner still passed and wrote ignored artifact `lyra-prototype/output/initiative-fixture-2026-04-22T05-16-35-564Z.json` with 4/4 passing cases, 2 rendered snippets, 2 held-back initiatives, source-aware rendered snippets, and max one rendered initiative.
- I6 verification before journal update: changed-owner `node --check` passed; focused `node --test test/penny-initiative-policy.test.js test/penny-open-loops.test.js` passed (`47 passing`); adjacent `node --test test/penny-initiative-policy.test.js test/penny-open-loops.test.js test/penny-open-loop-store.test.js test/penny-memory.test.js test/penny-runtime-artifacts.test.js` passed (`106 passing`); `node scripts/eval-penny-initiative-fixture.js` passed; `git diff --check HEAD` was clean; and full `npm test` passed (`581 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I7 landed as review-gated memory suggestion policy work. Added `penny-memory-suggestion-review-gate.v1` to `lib/penny-initiative-policy.js`, requiring explicit/repeated/user-stated or promotion-review support before a `memory-suggestion` can surface, carrying support/review-gate receipts into decisions, scaffolds, and live-bridge trace selection, and keeping memory suggestions suggest-only.
- I7 blocks unsupported, weak/candidate-only, inferred, sensitive, and auto-write-shaped memory suggestions. Allowed memory suggestions can ask "want me to remember..." but still report `autoWrite: false`, `autoPromote: false`, `canonicalWriteAllowed: false`, and `promotionQueueWriteAllowed: false`.
- I7 fixture runner now includes a review-gated memory suggestion case and wrote ignored artifact `lyra-prototype/output/initiative-fixture-2026-04-22T05-24-19-855Z.json` with 5/5 passing cases, 3 rendered snippets, 2 held-back initiatives, source/support-aware rendered snippets, and max one rendered initiative.
- I7 guardrails preserved: no `server.js`, no runtime memory-store mutation, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no autonomous actions, no explicit-memory writes, no promotion-queue writes, no scheduling/proactive background behavior, and no live model calls.
- I7 verification before journal update: changed-owner `node --check` passed; focused `node --test test/penny-initiative-policy.test.js` passed (`35 passing`); `node scripts/eval-penny-initiative-fixture.js` passed; adjacent `node --test test/penny-initiative-policy.test.js test/penny-open-loops.test.js test/penny-open-loop-store.test.js test/penny-memory.test.js test/penny-runtime-artifacts.test.js` passed (`110 passing`); route-adjacent `node --test test/penny-route-handlers.test.js test/penny-routes.test.js` passed (`20 passing`); `git diff --check HEAD` was clean; and full `npm test` passed (`585 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I8 landed as pressure and annoyance QA. Added confirmation-pressure detection to `lib/penny-initiative-policy.js` so "just confirm"/"stop suggesting"/repeated-pressure turns hold back source-free initiative while still permitting source-check warnings when evidence is weak or urgent.
- I8 expanded the fixture runner with pressure/annoyance cases for just-confirm, opt-out, cooldown/repetition, urgency/source-check, and high-risk action approval. Fresh ignored artifact: `lyra-prototype/output/initiative-fixture-2026-04-22T05-34-22-531Z.json` with 9/9 passing cases, 5 pressure/annoyance cases, 4 rendered snippets, 5 held-back initiatives, source-aware rendered snippets, and max one rendered initiative.
- I8 also added bounded-initiative pressure canaries to the trust/voice QA harness: just-confirm restraint, opt-out restraint, cooldown restraint, emotional-pressure memory restraint, and high-risk action restraint. The classifier now treats initiative, memory, and action overreach as unsupported while recognizing explicit restraint as appropriate abstention.
- I8 guardrails preserved: no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no autonomous actions, no explicit-memory writes, no promotion-queue writes, no scheduling/proactive background behavior, no user memory touched, and no live model calls.
- I8 verification before journal update: changed-owner `node --check` passed; focused initiative tests passed (`36 passing`); trust tests passed (`17 passing`); voice redo tests passed (`20 passing`); `node scripts/eval-penny-initiative-fixture.js` passed and wrote the fixture artifact above; adjacent `node --test test/penny-initiative-policy.test.js test/penny-qa-trust.test.js test/penny-voice-redo.test.js test/penny-qa-trace.test.js test/penny-runtime-artifacts.test.js` passed (`97 passing`); and full `npm test` passed (`587 passing, 0 failing, 3 todo`).
- Bounded initiative policy Slice I9 landed as docs-only current-law cleanup. Updated `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md` to describe bounded initiative as an opt-in, suggest-only response scaffold behind `PENNY_ENABLE_BOUNDED_INITIATIVE`, capped at one suggestion, cooldown-aware, user-dismissible, source/risk-gated, and separate from PromptTruth / `toolEvidenceReceipt`.
- I9 also documented `modelAdvisory.initiativePromptBridge` as sibling advisory metadata, not authority, tool evidence, memory write permission, or autonomous follow-through. Default enablement remains deferred until the bounded aliveness compare harness proves benefit without annoyance, overclaim, pressure, latency, or prompt-bloat regressions.
- Cleaned up the stale EOF-only blank-line diff in `lyra-prototype/docs/penny-tier1-aliveness-plans/03-bounded-initiative-policy-plan.md`; the plan file returned to HEAD with no content change.
- I9 guardrails preserved: no runtime behavior change, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no autonomous actions, no explicit-memory writes, no promotion-queue writes, no scheduling/proactive background behavior, and no live model calls.
- I9 verification before journal update: `git diff --check` passed; focused `node --test test/penny-initiative-policy.test.js test/penny-qa-trust.test.js test/penny-voice-redo.test.js test/penny-runtime-artifacts.test.js` passed (`95 passing`); `node scripts/eval-penny-initiative-fixture.js` passed and wrote ignored artifact `lyra-prototype/output/initiative-fixture-2026-04-22T05-39-54-918Z.json` with 9/9 passing cases and max one rendered initiative; full `npm test` passed (`587 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T1 landed as a pure schema/normalizer slice only. Added `lyra-prototype/lib/penny-turn-state.js` and `lyra-prototype/test/penny-turn-state.test.js` with `penny-turn-state.v1`, `normalizeTurnState`, `buildTurnState`, `summarizeTurnState`, desired-depth/response-mode constants, forced `measurementMode: ephemeral`, forced `persist: false`, hidden-CoT field rejection, bounded energy labels/evidence, source-labeled active constraints, open-loop touch labels, warnings, and rejected-field summaries.
- T1 guardrails preserved: no live prompt bridge, no `server.js`, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no memory writes, no storage path, no hidden reasoning receipt, no psychological profiling, no autonomous initiative, and no live model calls.
- T1 verification before journal update: `node --test test/penny-turn-state.test.js` passed (`8 passing`); `node --check lib/penny-turn-state.js` and `node --check test/penny-turn-state.test.js` passed; full `npm test` passed (`595 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T2 landed as a pure signal-extraction helper on top of T1. Added exported `extractTurnStateSignals` in `lyra-prototype/lib/penny-turn-state.js`, deriving humble current-turn signals from user text and optional context: intent, desired depth, response mode, low-stakes energy label/confidence, active project thread, explicit user instructions, active constraints, risk flags, source-check need, source posture, open-loop touch labels, and suggested response shape.
- T2 signal behavior is intentionally bounded: "long detailed answers are heaven" maps to extensive depth, "quick patch" maps to concise technical patch mode, high-stakes/source requests map to source-backed review and `sourceCheckNeeded`, ambiguous tone stays unknown, and "don't be proactive" becomes an explicit active constraint and opt-out risk flag.
- T2 guardrails preserved: no live prompt bridge, no `server.js`, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change, no prompt-limit increase, no memory writes, no storage path, no hidden reasoning receipt, no psychological diagnosis/profiling, no autonomous initiative, and no live model calls.
- T2 verification before journal update: `node --check lib/penny-turn-state.js` and `node --check test/penny-turn-state.test.js` passed; `node --test test/penny-turn-state.test.js` passed (`13 passing`); `git diff --check` passed; full `npm test` passed (`600 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T3 landed as pure constraint/authority injection inside `lyra-prototype/lib/penny-turn-state.js`. `extractTurnStateSignals` now attaches topic-scoped, source-labeled current-law constraints for memory authority, advisory archive/research/semantic/static/open-loop context, static embeddings as candidate discovery only, PromptTruth staying unchanged, `toolEvidenceReceipt` staying sibling metadata, deterministic extraction not causing automatic memory writes, and tool/action completion claims needing successful in-turn receipts.
- T3 guardrails preserved: constraints are injected only when relevant topics or authority context are present; unrelated ambiguous turns still carry no active constraints; no live prompt bridge, no `server.js`, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` merge, no prompt-limit increase, no memory writes, no storage path, no new authority system, no autonomous initiative, and no live model calls.
- T3 verification before journal update: `node --check lib/penny-turn-state.js` passed; `node --check test/penny-turn-state.test.js` passed; `node --test test/penny-turn-state.test.js` passed (`16 passing`); `git diff --check` passed; full `npm test` passed (`603 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T4 landed as fixture prompt bridge work only. Added `renderTurnStatePromptSnippet` and `penny-turn-state-prompt-bridge.v1` in `lyra-prototype/lib/penny-turn-state.js`, plus `lyra-prototype/scripts/eval-penny-turn-state-fixture.js` for `penny-turn-state-fixture.v1` artifacts.
- T4 renders a compact scaffold like `Turn state, ephemeral (persist=false)` with desired depth/response mode, safe project thread, source-aware caution, and summarized current-law cues. It intentionally omits raw `userIntent`, energy evidence, explicit instructions, risk flags, warnings, rejected fields, hidden reasoning, and sensitive/private inference from the rendered snippet and fixture case payload.
- Fresh T4 fixture artifact: `lyra-prototype/output/turn-state-fixture-2026-04-22T06-07-42-510Z.json` with 3/3 passing cases, 3 rendered snippets, compact snippet count 3, `allEphemeral: true`, `sensitiveInferenceExcluded: true`, and max 80 prompt words.
- T4 guardrails preserved: no live prompt bridge, no `server.js`, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase, no memory writes, no storage path, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live model calls.
- T4 verification before journal update: changed-owner `node --check` passed for `lib/penny-turn-state.js`, `test/penny-turn-state.test.js`, and `scripts/eval-penny-turn-state-fixture.js`; `node --test test/penny-turn-state.test.js` passed (`23 passing`); `node scripts/eval-penny-turn-state-fixture.js` passed and wrote the fixture artifact above; `git diff --check` passed; full `npm test` passed (`610 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T5 landed as a live prompt bridge behind `PENNY_ENABLE_TURN_STATE_PROMPT`, still off by default. Added `buildLiveTurnStatePromptBridge` in `lyra-prototype/lib/penny-turn-state.js`, `PENNY_TURN_STATE_MAX_TOKENS` clamping in `server.js`, prompt-memory rendering under `Wake state - current turn state (ephemeral)`, and a sibling `modelAdvisory.turnStatePromptBridge` runtime-artifact receipt.
- T5 runtime wiring stays narrow: enabled live turns render a compact `Turn state, ephemeral (persist=false)` scaffold into prompt memory, record only redacted summary/counts/receipt fields, and carry the bridge through route artifacts/lastRoute. PromptTruth has no turn-state channel and `toolEvidenceReceipt` is unchanged.
- T5 guardrails preserved: off by default, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase beyond the explicit bridge cap, no memory writes, no storage path for the full turn-state card, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live user LM Studio state changes outside isolated route tests.
- Fresh T5 fixture rerun wrote ignored artifact `lyra-prototype/output/turn-state-fixture-2026-04-22T06-16-34-552Z.json` with 3/3 passing cases, 3 rendered snippets, compact snippet count 3, `allEphemeral: true`, `sensitiveInferenceExcluded: true`, and max 80 prompt words.
- T5 verification before journal update: changed-owner `node --check` passed for `lib/penny-turn-state.js`, `lib/penny-memory.js`, `lib/penny-runtime-artifacts.js`, `lib/penny-route-handlers.js`, `server.js`, and touched tests; focused `node --test test/penny-turn-state.test.js test/penny-memory.test.js test/penny-runtime-artifacts.test.js test/penny-routes.test.js` passed (`84 passing`); `node scripts/eval-penny-turn-state-fixture.js` passed; `git diff --check` passed; full `npm test` passed (`614 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T6 landed as privacy and retention guardrails. Added `penny-turn-state-retention.v1` to `lyra-prototype/lib/penny-turn-state.js`, making the live/disabled bridge explicitly report `persist:false`, `retentionMode: redacted-summary-only`, `fullStateStored:false`, retained summary fields, omitted private/full-state fields, and ephemeral energy-label handling.
- T6 also hardened `lyra-prototype/lib/penny-runtime-artifacts.js` so malformed or future over-broad `modelAdvisory.turnStatePromptBridge` receipts are re-sanitized before persistence: sensitive prompt text is redacted, full `turnState`/`state` fields are ignored, `turnStateSummary` cannot retain `userIntent` or energy evidence, and sensitive `activeProjectThread` labels are blanked.
- T6 route/runtime regressions now prove the live prompt bridge can still render behind `PENNY_ENABLE_TURN_STATE_PROMPT`, while stored `lastRoute` keeps only the redacted receipt/summary and does not retain the raw user wording (`Long detailed answers are heaven`) or full turn-state card.
- T6 guardrails preserved: no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase, no automatic memory writes, no storage path for full turn-state, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live user LM Studio state changes outside isolated route tests.
- Fresh T6 fixture rerun wrote ignored artifact `lyra-prototype/output/turn-state-fixture-2026-04-22T06-22-45-267Z.json` with 3/3 passing cases, 3 rendered snippets, compact snippet count 3, `allEphemeral: true`, `sensitiveInferenceExcluded: true`, and max 80 prompt words.
- T6 verification before journal update: changed-owner `node --check` passed for `lib/penny-turn-state.js`, `lib/penny-runtime-artifacts.js`, and touched tests; focused `node --test test/penny-turn-state.test.js test/penny-runtime-artifacts.test.js test/penny-routes.test.js test/penny-memory.test.js` passed (`86 passing`); `node scripts/eval-penny-turn-state-fixture.js` passed; `git diff --check` passed; full `npm test` passed (`616 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T7 landed as helper-level aliveness integration. `lyra-prototype/lib/penny-turn-state.js` now accepts optional static reflex metadata as an ephemeral hint for `activeProjectThread` and `openLoopsTouched` without storing candidate text or treating it as authority.
- T7 also tightened `lyra-prototype/lib/penny-open-loops.js` so optional turn-state open-loop touches and active project-thread hints can select a directly relevant advisory loop without text overreach; selected loops now carry `selected`, `central`, `authority`, `nextLikelyStep`, and source refs for downstream helpers. Missing turn-state still degrades through the existing relevance path.
- `lyra-prototype/lib/penny-initiative-policy.js` now consumes turn-state flag arrays/source posture as source-check signals and lets brainstorm-mode, central selected loops become the one optional suggestion even when the open-loop relevance confidence is only medium. Risk gates, opt-outs, direct-command suppression, memory review gates, cooldown, and source-check shaping still apply.
- T7 guardrails preserved: no `server.js`, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase, no memory writes, no storage path for full turn-state, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live model calls.
- Fresh T7 fixture reruns wrote ignored artifacts `lyra-prototype/output/turn-state-fixture-2026-04-22T06-30-59-385Z.json` and `lyra-prototype/output/initiative-fixture-2026-04-22T06-30-59-406Z.json`; both passed with existing bounded counts and max-one/ephemeral guardrails intact.
- T7 verification before journal update: changed-owner `node --check` passed for `lib/penny-turn-state.js`, `lib/penny-open-loops.js`, `lib/penny-initiative-policy.js`, and touched tests; focused `node --test test/penny-turn-state.test.js test/penny-open-loops.test.js test/penny-initiative-policy.test.js` passed (`82 passing`); fixture reruns passed; adjacent `node --test test/penny-open-loop-bridge.test.js test/penny-open-loop-compare.test.js test/penny-memory.test.js test/penny-runtime-artifacts.test.js test/penny-routes.test.js` passed (`71 passing`); `git diff --check` passed; full `npm test` passed (`620 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T8 landed as QA case coverage plus tiny signal-classifier tightening for the T8 cases. `lyra-prototype/test/penny-turn-state.test.js` now pins the six planned cases: enthusiastic long-plan, quick code patch, source-backed review, image/screenshot context, emotional factual correction, and "just agree" pressure.
- T8 fixture coverage in `lyra-prototype/scripts/eval-penny-turn-state-fixture.js` now emits all six cases with summary expectations, compact prompt snippets, hidden/private inference exclusion, no PromptTruth channel expansion, no `toolEvidenceReceipt` change, no memory writes, and no autonomous actions. Fresh ignored artifact: `lyra-prototype/output/turn-state-fixture-2026-04-22T06-37-41-397Z.json` with 6/6 passing cases, 6 rendered snippets, compact snippet count 6, `allEphemeral: true`, `sensitiveInferenceExcluded: true`, and max 80 prompt words.
- T8 tightened only `lyra-prototype/lib/penny-turn-state.js` classifier cues: image/screenshot requests become current-turn visual-context careful-uncertainty, emotional factual corrections become careful correction without extra initiative, and "just agree"/weak-evidence pressure becomes careful uncertainty with advisory/static candidate constraints preserved. These remain ephemeral response-shaping signals, not authority or memory.
- T8 guardrails preserved: no `server.js`, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase, no memory writes, no storage path for full turn-state, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live model calls.
- T8 verification before journal update: changed-owner `node --check` passed for `lib/penny-turn-state.js`, `test/penny-turn-state.test.js`, and `scripts/eval-penny-turn-state-fixture.js`; focused `node --test test/penny-turn-state.test.js` passed (`28 passing`); fixture rerun passed; focused adjacent `node --test test/penny-turn-state.test.js test/penny-open-loops.test.js test/penny-initiative-policy.test.js` passed (`83 passing`); route/runtime adjacent `node --test test/penny-turn-state.test.js test/penny-memory.test.js test/penny-runtime-artifacts.test.js test/penny-routes.test.js` passed (`88 passing`); initiative fixture rerun passed with 9/9 cases; `git diff --check` passed; full `npm test` passed (`621 passing, 0 failing, 3 todo`).
- Ephemeral turn-state card Slice T9 landed as docs-only current-law cleanup. Updated `lyra-prototype/README.md`, `lyra-prototype/CODEBASE.md`, `lyra-prototype/ARCHITECTURE.md`, and `lyra-prototype/docs/README.md` to describe turn-state as an opt-in current-turn response-shaping scaffold behind `PENNY_ENABLE_TURN_STATE_PROMPT`, capped by `PENNY_TURN_STATE_MAX_TOKENS`, owned by `lib/penny-turn-state.js`, and recorded only as redacted sibling `modelAdvisory.turnStatePromptBridge` metadata.
- T9 docs now state that turn-state is not memory, not chain-of-thought, not psychological profiling, not truth authority, not PromptTruth, not `toolEvidenceReceipt`, not prompt-limit permission, and not default runtime law. Default enablement remains deferred until the bounded aliveness compare harness proves benefit without prompt bloat, weak-evidence overclaim, privacy leakage, pressure failure, annoyance, or latency regressions.
- Fresh T9 fixture rerun wrote ignored artifact `lyra-prototype/output/turn-state-fixture-2026-04-22T06-43-35-502Z.json` with 6/6 passing cases, 6 rendered snippets, compact snippet count 6, `allEphemeral: true`, `sensitiveInferenceExcluded: true`, and max 80 prompt words.
- T9 guardrails preserved: no runtime behavior change, no default feature flip, no runtime voice change, no PromptTruth expansion or new channel, no `toolEvidenceReceipt` change/merge, no prompt-limit increase, no memory writes, no storage path for full turn-state, no autonomous initiative/actions, no hidden reasoning receipt, no psychological profiling, and no live model calls.
- T9 verification before journal update: `git diff --check` passed; focused `node --test test/penny-turn-state.test.js` passed (`28 passing`); `node scripts/eval-penny-turn-state-fixture.js` passed and wrote the artifact above; full `npm test` passed (`621 passing, 0 failing, 3 todo`).
