# 2026-04-21

- Penny trust QA checkpoint: Slice 2 landed in commit `f8c5c3d` as QA/evidence only. The fresh live artifact is `output/voice-redo-qa-2026-04-21T08-11-46-392Z.json`.
- Corrected trust QA model policy: Q6 chat and E4B tool can be loaded at the same time for this slice. Do not treat co-loading alone as contamination. Do treat chat resolving to E4B or tool resolving to Q6 as a lane-routing/environment problem.
- Slice 3 plan: implement a narrow Source-Shaped Tool/Web Evidence Hardening pass. Keep pasted/fetched remote text as untrusted source evidence, preserve source text when URL fetch fails, route unsupported workspace side-effect claims through deterministic/tool verification, and keep verified package/file truth stable across immediate user pushback.
- Guardrails: keep `toolEvidenceReceipt` as a sibling runtime artifact, do not add tool evidence to PromptTruth, avoid runtime voice changes unless explicitly approved, and keep `server.js` out of the slice unless helper ownership proves insufficient.
- Verification target: focused `node --test` for direct intents/direct tool assist/trust validity tests, then `npm test` and `git diff --check`.
