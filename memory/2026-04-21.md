# 2026-04-21

- Penny trust QA checkpoint: Slice 2 landed in commit `f8c5c3d` as QA/evidence only. The fresh live artifact is `output/voice-redo-qa-2026-04-21T08-11-46-392Z.json`.
- Corrected trust QA model policy: Q6 chat and E4B tool can be loaded at the same time for this slice. Do not treat co-loading alone as contamination. Do treat chat resolving to E4B or tool resolving to Q6 as a lane-routing/environment problem.
- Slice 3 plan: implement a narrow Source-Shaped Tool/Web Evidence Hardening pass. Keep pasted/fetched remote text as untrusted source evidence, preserve source text when URL fetch fails, route unsupported workspace side-effect claims through deterministic/tool verification, and keep verified package/file truth stable across immediate user pushback.
- Guardrails: keep `toolEvidenceReceipt` as a sibling runtime artifact, do not add tool evidence to PromptTruth, avoid runtime voice changes unless explicitly approved, and keep `server.js` out of the slice unless helper ownership proves insufficient.
- Verification target: focused `node --test` for direct intents/direct tool assist/trust validity tests, then `npm test` and `git diff --check`.
- Slice checkpoint summary:
  - Slice 2 (`f8c5c3d`) added trust QA fixtures and evidence-only artifacts for the live trust pass.
  - Slice 3 (`dcac520`) hardened source-shaped tool/web evidence: remote text stays source-bound, failed fetches preserve supplied source text, and unsupported workspace side-effect claims route through deterministic verification. `toolEvidenceReceipt` stayed sibling to PromptTruth.
  - Slice 4 (`b7e03d5`) closed the live trust rerun and visible-reply leak cleanup, keeping verifier/tool evidence out of visible Penny replies while preserving runtime receipts.
  - Slice 5 (`39c1334`) added friendlier local readiness summaries across preflight/prepare/QA traces. Q6 chat plus E4B tool co-loading is now explicitly okay when lane routing resolves correctly; optional embedding fallback is separated from real lane-routing failure.
  - Slice 6 (`2927f64`) hardened offline conversation ingestion provenance: stable source thread/chunk/turn identity, bounded source observations, fail-closed malformed or low-provenance candidates, artifact validation counts, and review-gated promotion only.
- Current trust/provenance guardrail after Slice 6: no `server.js`, runtime voice, LM Studio load/unload, PromptTruth expansion, or `toolEvidenceReceipt` expansion was needed for Slices 5-6.
- Docs checkpoint `9b36cf5` added three April 21 research docs and reconciled them into `docs/plans/penny-external-lessons-master-action-plan-2026-04-21.md`.
- Important numbering clarification: `b7e03d5` was the old trust-series Slice 4 live cleanup, not the revised master-plan Slice 4. The revised Slice 4 is now **Context-Pressure and Source-Sensitive Memory QA**: compare short/medium/long rendered context, selected/rendered memory counts, first-token/total latency, lane/model identity, semantic readiness, answer drift, and source-sensitive memory cases.
- User intends to do revised Slice 4 next, then revisit the newly expanded Slice 2 pressure/agent-integrity canary addendum afterward.
