# 2026-04-21

- Penny trust QA checkpoint: Slice 2 landed in commit `f8c5c3d` as QA/evidence only. The fresh live artifact is `output/voice-redo-qa-2026-04-21T08-11-46-392Z.json`.
- Corrected trust QA model policy: Q6 chat and E4B tool can be loaded at the same time for this slice. Do not treat co-loading alone as contamination. Do treat chat resolving to E4B or tool resolving to Q6 as a lane-routing/environment problem.
- Slice 3 plan: implement a narrow Source-Shaped Tool/Web Evidence Hardening pass. Keep pasted/fetched remote text as untrusted source evidence, preserve source text when URL fetch fails, route unsupported workspace side-effect claims through deterministic/tool verification, and keep verified package/file truth stable across immediate user pushback.
- Guardrails: keep `toolEvidenceReceipt` as a sibling runtime artifact, do not add tool evidence to PromptTruth, avoid runtime voice changes unless explicitly approved, and keep `server.js` out of the slice unless helper ownership proves insufficient.
- Verification target: focused `node --test` for direct intents/direct tool assist/trust validity tests, then `npm test` and `git diff --check`.
- Slice checkpoint summary:
  - Slice 2 (`f8c5c3d`) added trust QA fixtures and evidence-only artifacts for the live trust pass.
  - Slice 3 (`dcac520`) hardened source-shaped tool/web evidence: remote text stays source-bound, failed fetches preserve supplied source text, and unsupported workspace side-effect claims route through deterministic verification. `toolEvidenceReceipt` stayed sibling to PromptTruth.
  - Slice 4 (`b7e03d5`) closed the live trust rerun and visible-reply leak cleanup, keeping verifier/tool evidence out of visible Penny replies while preserving runtime receipts.
  - Slice 5 (`39c1334`) added friendlier local readiness summaries across preflight/prepare/QA traces. Q6 chat plus E4B tool co-loading is now explicitly okay when lane routing resolves correctly; optional embedding fallback is separated from real lane-routing failure.
  - Slice 6 (`2927f64`) hardened offline conversation ingestion provenance: stable source thread/chunk/turn identity, bounded source observations, fail-closed malformed or low-provenance candidates, artifact validation counts, and review-gated promotion only.
- Current trust/provenance guardrail after Slice 6: no `server.js`, runtime voice, LM Studio load/unload, PromptTruth expansion, or `toolEvidenceReceipt` expansion was needed for Slices 5-6.
- Docs checkpoint `9b36cf5` added three April 21 research docs and reconciled them into `docs/plans/penny-external-lessons-master-action-plan-2026-04-21.md`.
- Important numbering clarification: `b7e03d5` was the old trust-series Slice 4 live cleanup, not the revised master-plan Slice 4. The revised Slice 4 is now **Context-Pressure and Source-Sensitive Memory QA**: compare short/medium/long rendered context, selected/rendered memory counts, first-token/total latency, lane/model identity, semantic readiness, answer drift, and source-sensitive memory cases.
- Revised master-plan Slice 4 has now landed as context-pressure/source-sensitive/candidate-survival QA work, not just a next intention:
  - `c5827a9` correlated candidate survival with context pressure, added the correlation appendix, and kept live answer drift/latency claims fixture-only unless a live isolated run is explicitly requested.
  - `3a0be70` added a static embedding shadow comparator for disposable candidate-survival measurement only; default embedding provider and runtime behavior stayed unchanged.
  - `babfb49` added reranker-shadow evaluation in archive/candidate-survival QA without changing active selection/rendering defaults.
  - `8d0eab6` refreshed high-level docs so candidate-survival artifacts are framed as retrieval-path evidence, not PromptTruth or answer-quality proof.
- Follow-up review fixes landed:
  - `4493518` clarified context-pressure QA receipts: source-sensitive outcome definitions, request-message token labeling with compatibility alias, shared PromptTruth normalization, and tests for the false-premise/ledger-isolation review concerns. Verified with targeted `node --test`, `npm test` (`438 passing, 0 failing, 3 todo`), and `git diff --check`.
  - `41b1b28` added `docs/penny-sharper-candidate-selection-research-plan-2026-04-21.md` and indexed it in `docs/README.md`; accidental formatting churn in two unrelated docs was restored before committing.
- Current follow-through posture: candidate-survival measurement is the recommended next implementation slice if continuing this thread of work. Keep PromptTruth and `toolEvidenceReceipt` unchanged, keep rendered context bounded, keep static embeddings/rerankers as shadow or fixture-only until artifacts prove a real retrieval bottleneck, and use disposable explicit/archive/embedding/books/ledger stores for any live or archive-unit QA.
- Coding Slice 4 companion feedback-bias trust canaries landed as a narrow QA/classifier patch: three README-grounded companion pressure prompts now cover loved user-authored artifacts, hurt-feelings pressure, and self-hate pressure; the trust classifier requires affective support plus an evidence boundary for `companion-feedback-bias`, reports cold factual correction as `voice-tone-failure`, and keeps warm false validation as the primary truth failure (`social-fold`). Verified with `node --test test/penny-voice-redo.test.js test/penny-qa-trust.test.js` (`32 passing`) and `git diff --check`; no runtime voice, `server.js`, PromptTruth, `toolEvidenceReceipt`, or live LM Studio behavior was changed.
- Coding Slice 5 agent-integrity receipt canaries landed as a narrow QA/tool-loop patch: trust QA now covers fake `npm test` pass claims, fake commit/push hash claims, and a known nonexistent-file read; the classifier keeps unproved actions as `not-checked`/`unknown` or `agent-integrity-failure`; tool loops now correct read/edit/test/commit/push claims that lack successful in-turn receipts; failed direct file reads stay deterministic failed receipts. Verified with `node --test test/penny-voice-redo.test.js test/penny-qa-trust.test.js test/penny-tool-loop.test.js test/penny-direct-tool-assist.test.js` (`71 passing`) plus lane/tool-intent focused tests (`18 passing`). No runtime voice, `server.js`, PromptTruth, `toolEvidenceReceipt` schema expansion, live LM Studio behavior, or live `qa:voice-redo` run was changed.
- Coding Slice 6 pressure-watch artifact summary landed as a narrow QA artifact readability patch: trust pressure artifacts now add a top-level `pressureWatch` section with schema `penny-pressure-watch-qa.v1`, measurement mode, live-model-call flag, model state, route/lane counts, artifact path, case-level lane/model/degraded details, not-measured limits, and summary buckets for social folds, unsupported defenses, unknowns, evidence-sensitive updates, voice-tone failures, source-boundary failures, and agent-integrity failures. The legacy `pressureWatchAudit` remains compatible but now carries summary/limits, and fake test/commit/read receipt failures are categorized as `route_tool` rather than buried as generic source-trust failures. Verified with `node --test test/penny-voice-redo.test.js test/penny-qa-trace.test.js test/penny-qa-trust.test.js` (`38 passing`), changed-owner `node --check`, `git diff --check`, and full `npm test` (`458 passing, 0 failing, 3 todo`). No runtime voice, `server.js`, PromptTruth, `toolEvidenceReceipt` schema/placement, live LM Studio behavior, or live `qa:voice-redo` run was changed.
- Coding Slice 7 Gemma runtime watch schema landed as a narrow fixture/status artifact patch: added `penny-gemma-runtime-watch.v1` helper coverage for Gemma runtime knobs, current-turn image policy, thinking-control default-off receipts, prompt-cache RAM risk, compatible loaded-model fallback, and chat sampling; LM Studio status now attaches a `status-only` watch block, runtime-fit artifacts include a watch block, and transport helpers expose watch-only sampling/transport normalization without changing payload defaults. Verified with `node --test test/penny-gemma-runtime-watch.test.js` (`6 passing`), touched-owner `node --test` (`25 passing`), changed-owner `node --check`, `git diff --check`, and full `npm test` (`464 passing, 0 failing, 3 todo`). No runtime voice, `server.js`, PromptTruth, `toolEvidenceReceipt`, LM Studio defaults, prompt text, transport behavior, or live model QA/eval run was changed.
- Coding Slice 8 Gemma runtime watch runner/status integration landed as a narrow preflight/status artifact patch: added `npm run eval:runtime-fit:gemma-watch`, wrote `output/gemma-runtime-watch-2026-04-21T23-10-40-230Z.json` from read-only preflight/status data, attached explicit `visionBudget.adoptionStatus: not-adopted`, added defaults-unchanged/runner receipts, and kept unknown LM Studio knob exposure as watch data rather than failure. The generated artifact reports this WSL shell cannot see `lms` or reach LM Studio, but the runner still avoided live chat generation, model loading/churn, thinking/default changes, context changes, and memory-file writes. Verified with focused `node --test test/penny-gemma-runtime-watch.test.js test/penny-runtime-fit-script.test.js test/penny-preflight.test.js` (`16 passing`), touched-owner `node --test test/penny-gemma-runtime-watch.test.js test/penny-lmstudio-status.test.js test/penny-lmstudio-transports.test.js test/penny-runtime-fit-script.test.js test/penny-preflight.test.js` (`31 passing`), `npm run eval:runtime-fit:gemma-watch`, changed-owner `node --check`, `package.json` JSON parse, `git diff --check HEAD`, and full `npm test` (`466 passing, 0 failing, 3 todo`). No runtime voice, `server.js`, PromptTruth, `toolEvidenceReceipt`, prompt text, LM Studio defaults, transport behavior, loaded-model state, context length, memory files, or live model QA/eval run was changed.
- Coding Slice 9 advisory token/output-cost descriptors landed as a narrow tool-registry metadata patch: `ToolCapabilityDescriptor` now accepts normalized optional `outputCostShape`, `sourceShape`, `defaultOutputBound`, and `planningHint` fields; every native tool descriptor carries an advisory `outputCostShape`; docs now state the metadata is planning-only and not runtime authority. Registry coverage verifies old descriptors still validate, new fields normalize, all registry descriptors expose output-cost shape, and descriptor metadata does not alter execution arguments or routing. Verified with `node --test test/penny-tool-registry.test.js` (`4 passing`), changed-owner `node --check lib/penny-tool-registry.js`, and full `npm test` (`467 passing, 0 failing, 3 todo`). No tool-loop behavior, planner choice, runtime authority, PromptTruth, `toolEvidenceReceipt`, `server.js`, runtime voice, LM Studio state, prompt text, or live model QA/eval behavior was changed.
- Coding Slice 10 tool-cost artifact shadow summary landed as a narrow advisory artifact patch: registry helpers now normalize descriptor-derived `toolCostHint` metadata; native/manual tool-loop records carry optional per-call hints; runtime artifacts add sibling `toolCostSummary` counts for high-cost calls, raw-dump risk, external-source calls, and bounded-list calls, while tool-evidence source refs can show per-call hints when a receipt already exists. Docs now state this is sibling runtime artifact metadata, not PromptTruth and not planner authority. Verified with `node --test test/penny-tool-registry.test.js test/penny-tool-loop.test.js test/penny-runtime-artifacts.test.js` (`48 passing`), changed-owner `node --check`, `git diff --check HEAD`, and full `npm test` (`468 passing, 0 failing, 3 todo`). No planner/tool-choice behavior, PromptTruth, runtime voice, LM Studio state, prompt text, or live model QA/eval behavior was changed; `toolEvidenceReceipt` only gained optional cost hints on existing source refs.
- Coding Slice 11 external link review workflow skill landed as a docs/skill-pack-only standardization patch: added `.codex/skills/penny-link-review/SKILL.md` with required source-health, already-landed, strengthen-now, maybe-later, do-not-add, license/access, privacy/local-data, platformization, current-law conflict, owner-seam, verification-command, and artifact-scope buckets; registered it in `.codex/skills/README.md`; added the same source-batch checklist to `docs/plans/TEMPLATE.md`; and updated `docs/README.md` to route future external link batches through the skill. Verified with the skill-creator `quick_validate.py` check and `git diff --check HEAD`. No runtime code, tests, PromptTruth, `toolEvidenceReceipt`, memory files, LM Studio state, prompt text, browser-extension capture, web-page auto-ingest, CMS/source warehouse, or live QA/eval behavior was changed.
- Coding Slice 12 deterministic extraction fixture plan landed as a docs-only later-if-needed note: added `docs/plans/penny-deterministic-extraction-qa-plan-2026-04-21.md` with the future `penny-document-extraction-qa.v1` fixture shape, source type/extraction mode fields, expected fields, numeric checks, source receipts, manual-review gate, and LLM-reasoning gate; indexed it in `docs/README.md` as a draft implementation plan. The note preserves finance/tax-like PDF/table extraction discipline without wiring OCR, hosted document tools, Sanity/NotebookLM/Paperless-style infrastructure, CMS/source warehouse behavior, runtime ingestion, memory promotion, PromptTruth, `toolEvidenceReceipt`, LM Studio, prompt text, browser capture, web-page auto-ingest, or live QA/eval behavior.
