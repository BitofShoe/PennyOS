# Penny Sidecar RAG Patterns

A tiny local RAG sandbox should separate document says from model infers. The useful pattern is document chunk provenance plus review-gated memory promotion, not private document import.
