## Archived snapshot

This folder is only for historical reference.

- `server.snapshot.txt` is a frozen copy of an older backend state.
- It is intentionally **not** named `server.js` anymore, because that was too easy to mistake for live code during search and review.
- Use git history first if you just need to answer "when did this change?"

Current live backend: [../../server.js](../../server.js)
